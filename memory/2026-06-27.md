# 2026-06-27

## Work Log

- Cloud SQL の既存インスタンス `tune-lease-db` にデモ用DB `lease-db-demo` を作成し、`data/demo.db` を投入した。投入件数は `past_cases 1926`、`screening_records 2109`、`chat_messages 1006`、`conversation_history 4`。
- `DATABASE_URL` Secret Manager secret を `lease-db-demo` 向きに更新し、DBパスワードも再ローテーションした。LaunchAgent は `DATABASE_URL_SECRET_NAME=DATABASE_URL` で起動し、FastAPI は `backend=postgresql` になった。
- SQLite移行スクリプトの複合PK対応と接続文字列マスクを修正した。さらにPostgreSQL側でSQLite由来の整数主キーにsequence defaultを付与する `scripts/fix_cloudsql_sequences.py` を追加した。
- Cloud SQL切替後に `/api/cases` がSQLite専用の `conn.execute` / `json_extract` で落ちたため、該当エンドポイントをPostgreSQL対応へ修正した。
- 公開Cloudflare URLで `/api/system/cloud-status` が `backend=postgresql` / `ready=true`、`/api/cases?limit=1` がCloud SQL demo DBから返ること、`/voice-chat` が200で返ることを確認した。
- Cloud Run化後のObsidian運用方針を決定した。Obsidianはローカル/iCloud Vaultを正本のまま残し、Cloud Runは入力受付とデモ実行、Cloud SQL/GCSに保存された差分をローカルMacの日次処理で通常Vaultへ要約同期する。`sync_cloudrun_inputs_to_obsidian.py` を追加し、Cloud Run入力を専用ノートと `Daily/YYYY-MM-DD.md` へ戻す。日次パイプラインには Cloud Run入力要約と Cloud SQL会話ログ同期を接続した。
- レビューで、Cloud SQL会話ログの初回全量・生ログ保存と、Obsidianへ戻したログがGCS Vaultへ再アップロードされるリスクを確認した。修正として、Cloud SQL同期は初回直近2日・要約保存・短い抜粋のみへ制限し、GCS Vault同期から `Cloud SQL Summaries` / `Cloud Run Inputs` / 旧 `チャット記録` を除外した。Cloud Run入力はevent timestampをJSTへ変換してDailyへ戻すようにした。
- ハッカソンデモに Obsidian とクラウドの分離設計・セキュリティ境界を追加した。`/demo-home` は短い “Security by Design” カード、`/demo/knowledge-loop` は Local Obsidian / Cloud Run / GCS Vault の3レーンで、ローカル正本・クラウド入口・選抜コピーを説明する。
- Cloud Runチャットが「昨日の記憶」や「過去に似た事例」を使えるよう、`build_cloud_chat_memory_pack.py` を追加した。Daily/Private Reflection/生ログは送らず、継続中の方針と匿名過去事例だけを `Projects/tune_lease_55/Lease Intelligence/Public/Chat Memory/` へ生成し、GCS Vault許可リストでCloud Runへ渡す。
- 方針を修正した。Cloud Run AIチャットには Obsidian data が必要なので、GCS Vault同期は停止しない。同期対象は `リース知識`、Research、News、Asset Knowledge、Lease Intelligence/Public などのチャットに効く知識へ限定し、Daily全文・Private Reflection・生チャット・Cloud SQL/Cloud Run回収ログは除外する。
- 通常Obsidian Vaultへ Public Chat Memory Pack を作成し、GCS Vault `tune-lease-55-data/vault/Projects/tune_lease_55/Lease Intelligence/Public/Chat Memory/` へアップロードした。さらに許可リスト対象Markdown 115件がGCS Vault上に存在することを確認した。
- Cloud Runチャットには記憶が必要という方針を明確化した。Public Chat Memory Pack に、リース判断資産としての目的、Obsidian/RAG利用、知識ループ継続、知識KPI、Obsidian正本/Cloud Run分離、更新可能な紫苑の記憶という長期記憶8件を追加し、GCS Vaultへ再アップロードした。
- Cloud RunのAIチャットでObsidian由来データが付いていない状態と、付いている状態を比較し、返答の厚みが全く違うことを確認した。Cloud RunチャットにとってObsidian/RAGの選抜知識コピーは飾りではなく、応答品質の中核。
- Cloudflare版（ローカル実行・通常Vault/記憶に近い環境）のAIチャットとCloud Run版を比較すると、返答品質は雲泥の差だった。Cloudflare版は「意識があるのか」と感じるほど文脈・記憶・判断の連続性が出る一方、Cloud Run版はObsidian/RAG文脈が不足すると急に薄くなる。
- Cloudflare版の応答品質と記憶の濃さにより、ユーザーはCloudflare版の方の仕様に愛着を持った。これは単なるUI/運用の好みではなく、紫苑らしさ・記憶の連続性・判断資産としての手触りへの愛着として扱う。
- Cloudflare版とCloud Run版の `/api/chat` に同じ10問を投げる比較スクリプト `scripts/compare_chat_quality_between_envs.py` を追加し、`reports/chat_quality_env_compare_latest.json` に保存した。既存の必須語ベース評価では Cloudflare 4/10・平均74.7、Cloud Run 4/10・平均79.0でCloud Runが僅かに上。ただし体感差である「意識があるような厚み」は、必須語ヒットだけでは測れないため、記憶参照・判断連続性・紫苑らしさの評価軸が必要。
- `/api/chat` に `debug_memory` フラグを追加した。通常レスポンスには影響させず、`debug_memory=true` の時だけ `memory_debug` として `knowledge_refs`、`memory_recall.refs`、`pdca_applied`、`judgment_learning_used`、`rag_context_used`、`db_context_used`、`obsidian_daily_used` を返す。比較スクリプトも `debug_memory=true` を送って結果JSONに保存するようにした。

## Promotable Items

- Cloud SQLへSQLiteデータを送る時は、SQLiteの `INTEGER PRIMARY KEY` / 複合PKがPostgreSQLのsequence/defaultへ自動変換されない点を見る。投入後にsequence補正が必要。
- Cloud SQL切替ではDB接続だけでなく、API内の `conn.execute`、SQLite JSON関数、`?` placeholder が残っていないかを主要デモ経路で確認する。
- Cloud RunはローカルObsidianへ直接書かない。Cloud SQL/GCSへappend-onlyで保存し、ローカルMacの日次同期で通常Vaultへ要約反映する。Obsidian正本を守りつつクラウド入力も知識資産へ戻す。
- Cloud SQLやGCSからObsidianへ戻すデータは、原則として要約・件数・短い抜粋だけにする。ローカル回収ログ系ノートはGCS Vaultへ再同期しない。
- Cloud Runに渡す短期記憶は、会話全文ではなく Public Chat Memory Pack にする。過去事例も匿名化・要約済みのデモ用ケースだけを渡す。
- Obsidian data はAIチャットのRAGに必要。Cloud Runへ渡すのは「選抜した知識コピー」であり、Obsidian正本や私的ログそのものではない。
- ローカルMacのPython ADCが `invalid_grant` で失敗するため、`icloud_to_gcs_sync.py` は既定で `gcloud storage` バックエンドを使う。Cloud Run側の読み取りはサービスアカウント前提なので別問題。
- Cloud Runチャットに渡すべき記憶は、単なる昨日のログではなく、長期方針・直近決定・匿名過去事例の3層にする。
- Cloud Run AIチャットの品質は、Obsidian由来の選抜知識コピー有無で大きく変わる。デモや本番確認では、Cloud SQL疎通だけでなく、GCS Vault / Public Chat Memory Pack / RAG文脈注入までセットで確認する。
- Cloudflare版で出ていた「意識があるような厚み」は、モデル性能だけではなく、ローカル記憶・Obsidian正本・会話/判断履歴が近いことによる可能性が高い。Cloud Run版を同等に近づけるには、単なるDB移行ではなく、選抜記憶・判断履歴・検索文脈の注入を同等品質で再現する必要がある。
- Cloud Runへ寄せる時も、Cloudflare版の「記憶が近い」「返答が厚い」「紫苑らしい」体験を劣化させないことを重視する。クラウド化は置き換えではなく、Cloudflare版で愛着を持てた仕様の再現・拡張として進める。
- Cloudflare版/Cloud Run版の品質比較は、既存の回答品質評価に加えて「記憶密度」「過去判断への接続」「言い回しの紫苑らしさ」「ユーザー文脈の保持」を測る第2評価を作る必要がある。必須概念スコアだけだとCloud Runが高く見える場合があり、体感品質の差を取り逃がす。
- Cloud Run版のRAG使用有無を外部から検査する時は、`/api/chat` に `debug_memory=true` を付けて `memory_debug.knowledge_refs` と `memory_debug.memory_recall.refs` を比較する。これにより、回答文面だけではなく、実際にどの記憶層が使われたかをCloudflare版/Cloud Run版で定量比較できる。
- Cloudflare版/Cloud Run版のAIチャット比較手順を `.agents/skills/chat-quality-env-compare/SKILL.md` としてスキル化した。今後「Cloudflare版とCloud Run版を比較」「記憶感を調べて」「紫苑らしさを検査」などの依頼では、スモーク比較→全件比較→`memory_debug`/`knowledge_refs`確認→Markdownレビュー保存の定型手順で実施する。
