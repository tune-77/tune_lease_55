# 2026-06-06

## Restart at 02:17 JST
- User wrote `再起的`; interpreted as restart request.
- Ran `FORCE_RESTART=1 PUBLIC_TUNNEL=1 API_HOST=127.0.0.1 NEXT_HOST=127.0.0.1 bash run_next_stable.sh`.
- Launcher skipped frontend build because no frontend source changed since last successful build.
- Verified FastAPI `/docs`, Next `/`, local `/home`, and Cloudflare `/home`.
- Current quick tunnel URL: `https://asked-mistakes-minneapolis-leone.trycloudflare.com/home`.

## Next/Cloudflare restart and home page incident
- User requested app restart. Used `restart-next-cloudflare` workflow with `FORCE_RESTART=1 PUBLIC_TUNNEL=1 API_HOST=127.0.0.1 NEXT_HOST=127.0.0.1 bash run_next_stable.sh`.
- Initial stack started successfully: FastAPI `http://127.0.0.1:8000`, Next `http://127.0.0.1:3000`, Cloudflare quick tunnel.
- User reported that the home page would not open. HTTP checks showed `/` and `/home` returned `200 OK`; API logs also showed `/api/dashboard/stats`, `/api/lease-news/recent`, and related endpoints returning `200`.
- Root cause: `frontend/src/app/home/page.tsx` rendered a full-screen `loading` state (`データを集計中...`) until dashboard stats finished. This made the home page appear unavailable even though server/API routing was healthy. Cloudflare URL changes and browser/service-worker cache made diagnosis slower.
- Fix applied: removed the full-screen early `if (loading) return ...` gate and changed the status badge to `ONLINE & SYNCING` while data is still loading, so the home dashboard shell opens immediately.
- Verification: `npm run build` passed; local `/home`, API docs, and Cloudflare `/home` responded successfully. New public URL at that time: `https://goat-automated-media-aaa.trycloudflare.com/home`.

## Future countermeasures
- When user says "home won't open", do not stop at `curl 200`. Check whether the page is returning a loading-only shell or the actual home content.
- Prefer this triage order: `curl -i /home`, Next log, API log for `/api/dashboard/stats`, current Cloudflare tunnel log URL, then frontend code for full-screen loading/error gates.
- For Cloudflare quick tunnels, always refresh the URL from the newest `logs/next/tunnel_*.log`; old trycloudflare URLs are disposable.
- If local sandbox `curl` cannot reach `127.0.0.1:3000` while `next-server` is alive, retry with approved/escalated `curl --max-time` before assuming Next is down.

## Gunshi Gemini strategy visibility fix
- User reported that the Gemini-linked Gunshi panel did not show the initial request text: `この案件（スコア 100.0点、D 建設業）の稟議を通すための「逆転戦略」を教えてくれ！`.
- Root cause: the text was added to chat history, but strategy cards rendered above chat history and could hide the user request; Gemini streaming could also end after a very short partial answer, leaving little usable strategy text.
- Fix applied: `frontend/src/components/analysis/GunshiAdvice.tsx` now builds one reusable `initialStrategyQuestion`, shows it as a fixed `今回の問い` block above the strategy board, and auto-opens the strategy board when `strategy_cards` arrive.
- Fix applied: `api/gunshi_gemini.py` now accumulates Gemini stream text and appends deterministic `軍師AI 代替戦略` when Gemini returns fewer than 120 chars.
- Verification: `python -m py_compile api/gunshi_gemini.py` passed; `npm run build` passed; `/api/gunshi/stream` returned bayes, phrases, strategy cards, short Gemini text, fallback strategy, then `done`.

## FastAPI score/full socket hang-up
- User reported frontend alert: `審査エンジンの呼び出しに失敗しました。FastAPIサーバーが起動しているか確認してください。`
- Logs showed Next proxy `socket hang up` for `/api/score/full`. FastAPI `/docs` could recover, but the API process had previously hit `Segmentation fault: 11`; later score requests returned `200` while the process briefly became hard to reach.
- Root cause narrowed: `/api/score/full` calls Streamlit-era `components.score_calculation.run_scoring()`, which in API mode still triggered sidecar `screening_recorder` and `FluidPipeline` hooks. Those hooks are redundant because `api.main` saves the case separately, and they increase FastAPI instability by pulling in extra background/ML work.
- Fix applied: `components/score_calculation.py` now skips `screening_recorder` and `FluidPipeline` when `form_result["_api_mode"]` is true.
- Additional hardening: `api/gunshi_gemini.py` now skips Gunshi RAG by default unless `ENABLE_GUNSHI_RAG=1`, avoiding local embedding-model loading in the shared FastAPI process during normal use.
- Verification: `python -m py_compile components/score_calculation.py api/gunshi_gemini.py` passed. After restart, `/api/score/full` through Next returned `HTTP 200` three times consecutively and `/docs` returned `200`.

## Home latest lease news not showing
- User reported the home page latest lease news area was empty.
- Root cause: `/api/lease-news/recent` only read `Obsidian Vault/リースニュース`, but collected news notes were actually under `Obsidian Vault/05-クリップ_記事/リースニュース`.
- Fix applied: `api/main.py` now uses `05-クリップ_記事/リースニュース` as the primary news folder and keeps the old root-level `リースニュース` path as a readable alias.
- Verification: `/api/lease-news/recent?limit=5` returned saved news items including 2026-06-06 lease news; Cloudflare `/api/lease-news/recent?limit=2` returned items; Cloudflare `/home` returned `200`.

## Restart time reduction plan implemented
- User pointed out that restarts were taking too long and asked for a countermeasure based on logs.
- Root cause: the previous workflow defaulted to `FORCE_RESTART=1`, recreating FastAPI, Next, and Cloudflare quick tunnel even when only status or one service needed attention. Cloudflare URL churn and sandbox-local curl false negatives also caused unnecessary repeated checks.
- Fix applied: `run_next_stable.sh` now supports `RESTART_SCOPE=status`, `RESTART_SCOPE=api`, `RESTART_SCOPE=next`, and `RESTART_SCOPE=tunnel` before launcher lock handling. This allows quick status checks and targeted supervised child restarts without full stack teardown.
- Fix applied: full startup now prints the latest Cloudflare URL automatically instead of requiring a separate log scrape.
- Fix applied: `.agents/skills/restart-next-cloudflare/SKILL.md` now instructs future sessions to run status first, target only the failing service, and use full restart only as the fallback.
- Verification: `bash -n run_next_stable.sh` passed; `RESTART_SCOPE=status ... bash run_next_stable.sh` returned immediately with API/Next listener status and the latest tunnel URL.

## Cloud Run read-only bundle deployment
- User requested Cloud Run operation with SQLite and Obsidian treated as bundled master data.
- Fix applied: added `scripts/package_cloud_run_bundle.sh` to copy `data/lease_data.db`, `data/screening_db.sqlite`, and selected Obsidian folders into `.cloudrun_bundle/`.
- Fix applied: `Dockerfile` now bakes `.cloudrun_bundle/` into the image and `scripts/start_cloud_run.sh` seeds the runtime `/app/data` and `/app/obsidian_vault` directories from that snapshot.
- Fix applied: hardcoded `/Users/.../data` paths in app-facing modules were redirected to shared `DATA_DIR` helpers so the Cloud Run container reads the bundled snapshot instead of the developer machine path.
- Verification: `bash -n` on the Cloud Run scripts passed; `python -m py_compile` passed for the touched Python modules; local Cloud Run-style startup with `PORT=3103 FASTAPI_PORT=8103` returned `NEXT_OK` and `API_OK`, and both `/private/tmp/cloudrun-data2/lease_data.db` and `/private/tmp/cloudrun-vault2/Projects/tune_lease_55` were seeded from the bundle.

## Coefficient display overflow audit
- User reported that coefficient values sometimes overflow their visual containers and asked for all-screen confirmation.
- Scope checked: Next pages `/home`, `/quantitative`, `/qualitative`, and `/history-dash` at desktop `1440px`, tablet `900px`, and mobile `390px`.
- Root cause: long coefficient/feature labels in Recharts vertical axes and compact Top Driver rows lacked enough truncation/min-width handling. On mobile `/home`, `回帰係数: -0.7930` exceeded its text box by 4px in the first pass.
- Fix applied: `frontend/src/app/quantitative/page.tsx` and `frontend/src/app/qualitative/page.tsx` truncate chart axis labels with `tickFormatter`, widen Y axes to 170px, reduce tick font size, keep all ticks visible, and hide chart-container spillover.
- Fix applied: `frontend/src/app/home/page.tsx` and `frontend/src/app/history-dash/page.tsx` add `min-w-0`, truncation/title attributes, `tabular-nums`, and mobile-only spacing/font reductions so coefficient rows stay inside cards.
- Verification: `npm run build` passed. Playwright DOM overflow scan returned `overflowX: 0` and `totalOffenders: 0` across all 12 page/viewport combinations.
- Operational note: when Playwright or server bind fails inside the sandbox with `EPERM`, rerun the browser/server check with escalated local permissions before treating it as an app regression.
- User feedback: `かなり改善された`. Current truncation/min-width/mobile spacing approach appears directionally good and should be preserved when touching these screens again.

## Screening analysis information reduction
- User said the screening analysis screen has too much information and asked to remove areas that overlap with Gunshi AI chat.
- Policy set: the left analysis tab should focus on numeric/model evidence, charts, rate proposal, data source, and final report output. Gunshi AI should own strategy, approval tactics, questions to ask, risk wording, customer one-liners, and ringi memo text.
- Fix applied: `frontend/src/app/page.tsx` removed duplicate default analysis blocks: conditional approval actions, input reflection memo, approval comment draft, AI advice text, and advanced simulation UI.
- Verification: `npm run build` passed.

## README current-spec refresh
- User asked to update git README/read.md to the current specification.
- Fix applied: rewrote root `README.md` around the current Next.js + FastAPI primary architecture, `run_next_stable.sh`, Cloudflare quick tunnel, `RESTART_SCOPE`, `/api/score/full`, current borrower model split, Q_risk semantics, Obsidian/news integration, analysis-screen information reduction policy, and git/data guardrails.
- Verification: documentation review only; no build needed.

## Screening analysis second simplification
- User selected the next priority: make the screening analysis screen even simpler.
- Fix applied: the default left analysis view now keeps only primary indicators, critical warnings, and the rate proposal visible.
- Moved Score DAG/Q_risk/UMAP/Mahalanobis, detailed graphs/data sources, and report output into three collapsed native `details` sections.
- The Gunshi AI right column remains responsible for strategy and narrative guidance.
- Verification: `npm run build` passed.

## Normal analysis and Gunshi AI responsibility split
- User selected the next priority: clarify the role split between Gunshi AI and normal analysis.
- Normal analysis tab renamed to `数値分析`; it owns scores, model evidence, warnings, rate proposal, graphs, and report output.
- Gunshi panel renamed to `軍師AI 戦略相談`; it owns next actions, predicted screening objections, customer questions, customer wording, ringi memo, and follow-up consultation.
- Removed the Bayesian probability gauge/factor list and similar-case fetch/list from Gunshi AI because they duplicated numeric analysis.
- Verification: `npm run build` passed.

## Obsidian search precision improvement
- User selected the next priority: improve Obsidian search precision.
- Query parsing now applies NFKC normalization, preserves Japanese long vowels, handles hyphen variants, removes conversational filler, and conservatively splits Japanese particles without breaking words such as `ものづくり`.
- Shared `mobile_app/obsidian_bridge.py` search now reranks vector and keyword candidates using title/path/body matches, primary-term coverage, domain aliases, and knowledge-path priority.
- Unrequested AI Chat/Daily/Humor notes are penalized; humor notes return normally when humor or tone is explicitly requested.
- Vector results outside the selected normal Obsidian Vault, including `lease-wiki-vault`, are excluded.
- Removed unrelated recent-note padding from AI context so missing search results do not inject arbitrary recent logs.
- Verification: real-Vault integrated RAG evaluation reached hit@5 10/10 with forbidden-note cases 0/10. Related tests passed 16/16; one unrelated pre-existing fallback wiki-save assertion still fails in the full test file.

## Judgment change feedback loop
- User selected the next priority: connect judgment-change logs to model improvement.
- Added `judgment_feedback.py` with a dedicated SQLite `judgment_feedback` table. It stores AI judgment, human final judgment, reason, score, redacted input snapshot, evidence snapshot, and review status.
- The `/debate` news judgment UI now requires an actual AI-to-human decision change and a reason before recording.
- Added candidate summary/list/review APIs. Only `approved` rows are returned by the default training-candidate loader.
- Human approval judgments remain separate from delinquency/default labels and are not injected into `retraining_pipeline.py`.
- Verification: judgment feedback and screening recorder tests passed 13/13; `npm run build` passed.
- Follow-up correction: user noted that judgments are not normally changed specifically because of news. The `/debate` input is now always shown after the AI decision, regardless of news availability, under `担当者の最終判断を記録`. News is optional evidence only.
- Added the same judgment-change input to the main screening screen's `軍師AI 戦略相談` panel. It uses the scoring result as the AI judgment and stores the strategy board, risk cards, today moves, and latest Gunshi reply as supporting evidence.

## Lease news AI classification and deduplication
- Extended `scripts/collect_lease_news_to_obsidian.py` so collected news is classified before Obsidian save. Gemini structured output is used when available; deterministic rules keep collection working when the API is unavailable.
- Each note now stores target industries, lease assets, credit-risk impact, screening checks, impact direction, source reliability, classification confidence, validity date, canonical topic, canonical URL, and dedup ID.
- Added an `AI審査分類` section so the classification content is included directly in Obsidian RAG chunks, not only in frontmatter metadata.
- Duplicate handling now normalizes tracking URLs, compares title similarity and canonical topics, and appends alternate coverage to the existing note's `関連報道` section.
- Changed the collector's default news destination to the active normal-Vault path `05-クリップ_記事/リースニュース`.
- Verification: news collector tests passed 4/4; frontmatter parsing and H2 RAG chunk generation were confirmed. Live RSS collection could not be verified in the sandbox because DNS access was blocked.

## Lease judgment Auto Research
- User redirected the knowledge strategy from news-first collection to research that directly supports lease decisions.
- Added `scripts/auto_research_lease_judgment.py`. It rotates through cash flow, residual value, accounting/tax, contract/ownership, industry risk, asset operation, subsidies, pricing, bank support, and collection topics.
- Research uses Gemini Google Search, requires source URLs, labels sources as primary/recognized/supplementary, and refuses to save a new note when no verifiable source is returned.
- Every saved note must contain conclusion, source quality, confirmed facts, screening application, questions, condition-changing signals, counter-evidence, and refresh conditions. Notes remain `needs_human_review`.
- Added the research task before news in `daily_knowledge_feed.py`. A dedicated `com.tunelease.lease-judgment-autoresearch` LaunchAgent runs only Auto Research every day at 06:10; the broader knowledge feed remains monthly.
- First note saved to the normal Vault at `Projects/tune_lease_55/Research/Auto Research/2026-06-06_cash-flow.md`; 12 sources were captured and 10 RAG chunks indexed.
- Verification: Auto Research and news tests passed 8/8; the dedicated LaunchAgent has a daily 06:10 trigger.
