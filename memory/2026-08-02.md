# 2026-08-02

## Codex Work Log

### Vertex AI Search / Agent Builder Pilot
- User wanted to use remaining `Genaiappbuilder` credits by testing Vertex AI Search / Agent Builder against Obsidian lease knowledge.
- Created a safe exporter: `scripts/export_obsidian_for_agent_search.py`.
  - Source: normal iCloud Obsidian Vault `Projects/tune_lease_55`.
  - Excludes chat logs, Cloud Run conversation logs, daily/private memory, raw feedback returns, and secret-like files.
  - Exports selected Research / Judgment Assets notes as text plus `manifest.json` and `documents.jsonl`.
- Uploaded pilot corpus to `gs://tune-lease-55-data/agent-search/lease-knowledge/`.
- Created Discovery Engine resources:
  - Data Store: `tune-lease-55-obsidian-lease`
  - Search App / Engine: `tune-lease-55-obsidian-search`
  - Engine config: `SEARCH_TIER_ENTERPRISE` + `SEARCH_ADD_ON_LLM`
- Imported 118 Obsidian-derived documents. Verified Data Store lists 118 unique documents.
- Added query helper: `scripts/query_vertex_agent_search.py`.
- Verified searches:
  - `再リース案件では耐用年数と残価をどう確認するべきか`
  - `補助金前提の工作機械リースで確認すべきこと`
- Added operation memo: `docs/vertex_ai_search_obsidian_pilot.md`.

### Next
- Compare Vertex AI Search results with existing local Obsidian RAG before wiring it into production chat.
- Keep this as a pilot path; do not replace the existing RAG route without evaluation.

### Obsidian RAG vs Vertex AI Search Comparison
- Added `scripts/compare_obsidian_rag_vertex_search.py` to run the same six lease-domain questions against:
  - existing local Obsidian RAG via `obsidian_ai_context.collect_obsidian_ai_context`
  - Vertex AI Search pilot engine `tune-lease-55-obsidian-search`
- Saved reports:
  - `reports/obsidian_rag_vs_vertex_search_20260802.md`
  - `reports/obsidian_rag_vs_vertex_search_20260802.json`
  - `reports/obsidian_rag_vs_vertex_search_latest.md`
  - `reports/obsidian_rag_vs_vertex_search_latest.json`
- Result summary: Vertex average keyword coverage 0.867 vs Obsidian 0.700. Vertex was stronger on broad subsidy/transport-risk questions and generated citation-style summaries. Existing Obsidian RAG was strongest for precise local asset notes, especially the machine-tool resale query.
- Next improvement before production wiring: deduplicate Vertex results by canonical topic/source path because repeated Auto Research notes can dominate results.

### Hybrid RAG Wiring
- Added `api/vertex_agent_search.py` as an optional supplementary Vertex AI Search client.
- Wired `api/main.py` `next_chat_rag` so existing local Obsidian RAG remains primary and Vertex AI Search context is appended only for lease-domain RAG turns.
- Added response/debug metadata under `vertex_ai_search` and `memory_debug.vertex_ai_search`.
- Added `tests/test_vertex_agent_search.py`; verified disabled mode and Auto Research deduplication.
- Fast rollback: set `VERTEX_AGENT_SEARCH_ENABLED=false`.

### Hybrid RAG Debug Verification
- Ran real `/api/chat` with `debug_memory=true` against a temporary local FastAPI instance on port 8010.
- Initial request exposed two unrelated `/api/chat` runtime bugs:
  - missing `_lease_news_brief_to_dict`
  - missing `_CHAT_JUDGMENT_ASSET_TRIGGERS` / `_CHAT_JUDGMENT_ASSET_ACTIONS`
- Fixed both in `api/main.py`.
- Verified the chat returned 200 OK and `memory_debug.vertex_ai_search.used=true`, `status=ok`.
- Vertex refs returned:
  - `Projects/tune_lease_55/Research/補助金　総合レポート.md`
  - `Projects/tune_lease_55/Research/補助金　対象要件と活用条件の整理_report.md`
  - `Projects/tune_lease_55/Research/補助金制度の全体像調査_report.md`

### Obsidian to Vertex Upload Automation
- Added `scripts/sync_obsidian_to_vertex_agent_search.py`.
- The script exports selected lease knowledge notes from the normal iCloud Obsidian Vault, syncs them to `gs://tune-lease-55-data/agent-search/lease-knowledge/`, and can trigger Discovery Engine `documents:import`.
- Ran dry-run successfully, then ran real sync/import without stale GCS deletion.
- Import operation completed successfully:
  - Operation: `projects/1020894094172/locations/global/collections/default_collection/dataStores/tune-lease-55-obsidian-lease/branches/0/operations/import-documents-18200604050144976794`
  - `successCount=119`
  - `totalCount=119`
- Verified query found the newly uploaded `Projects/tune_lease_55/Research/Auto Research/2026-08-02_asset-operation.md` note in Vertex AI Search.
- Wired the sync into the nightly post pipeline via `scripts/run_daily_improvement_post.sh`.
- Added export-signature state at `data/agent_search/vertex_obsidian_sync_state.json`; unchanged Obsidian corpus now skips GCS sync/import to avoid unnecessary Vertex work and cost.
- Pipeline step log name: `sync_obsidian_to_vertex_agent_search`.

### Screening Shion Review Vertex Assist
- Wired the screening analysis Shion review prompt to include `【Vertex補助検索ヒント】` so `/api/chat` can use a compact Vertex query instead of the full long review prompt.
- Added backend extraction `_extract_vertex_search_hint` in `api/main.py`; Vertex AI Search now uses the hint when present.
- Added frontend display chips on the screening review card for Vertex ON/status and top Vertex refs.
- Added a guard so screening review prompts and Vertex hint markers are not captured as manual judgment asset candidates.
- Verified real `/api/chat` with a screening-style prompt returned `memory_debug.vertex_ai_search.used=true`, `status=ok`, with subsidy-related Vertex refs.

### Vertex Answer API Grounding Support Score
- Added Answer API support to the hybrid Vertex path for screening review prompts with `groundingSpec.includeGroundingSupports=true`.
- Switched Answer API calls to Discovery Engine `v1beta`; official docs say this is where `groundingScore` / `groundingSupports` are returned when supported.
- Current Obsidian Data Store returned citations but no official `groundingSupports`, so the app now falls back to a citation-span coverage score and labels it with `grounding_score_source="citation_coverage_fallback"`.
- Verified `/api/chat` with `debug_memory=true`: `memory_debug.vertex_answer_api.used=true`, `status=ok`, `grounding_score=0.887`, `support_count=25`, `low_support_claim_count=0`.
- Removed `codex_screening_answer_debug` test rows from local JSONL logs after verification.
