# 2026-08-02

## Codex Work Log

### Vertex AI Search / Agent Builder Pilot
- User wanted to use remaining `Genaiappbuilder` credits by testing Vertex AI Search / Agent Builder against Obsidian lease knowledge.
- Created a safe exporter: `scripts/export_obsidian_for_agent_search.py`.
  - Source: normal iCloud Obsidian Vault `Projects/tune_lease_55`.
  - Excludes chat logs, Cloud Run conversation logs, daily/private memory, raw feedback returns, and secret-like files.
  - Exports selected Research / Judgment Assets notes as text plus `manifest.json` and `documents.jsonl`.
- Uploaded pilot corpus to `gs://tune-lease-55-data/agent-search/lease-knowledge/`.
- Created Discovery Engine resources:
  - Data Store: `tune-lease-55-obsidian-lease`
  - Search App / Engine: `tune-lease-55-obsidian-search`
  - Engine config: `SEARCH_TIER_ENTERPRISE` + `SEARCH_ADD_ON_LLM`
- Imported 118 Obsidian-derived documents. Verified Data Store lists 118 unique documents.
- Added query helper: `scripts/query_vertex_agent_search.py`.
- Verified searches:
  - `再リース案件では耐用年数と残価をどう確認するべきか`
  - `補助金前提の工作機械リースで確認すべきこと`
- Added operation memo: `docs/vertex_ai_search_obsidian_pilot.md`.

### Next
- Compare Vertex AI Search results with existing local Obsidian RAG before wiring it into production chat.
- Keep this as a pilot path; do not replace the existing RAG route without evaluation.

### Obsidian RAG vs Vertex AI Search Comparison
- Added `scripts/compare_obsidian_rag_vertex_search.py` to run the same six lease-domain questions against:
  - existing local Obsidian RAG via `obsidian_ai_context.collect_obsidian_ai_context`
  - Vertex AI Search pilot engine `tune-lease-55-obsidian-search`
- Saved reports:
  - `reports/obsidian_rag_vs_vertex_search_20260802.md`
  - `reports/obsidian_rag_vs_vertex_search_20260802.json`
  - `reports/obsidian_rag_vs_vertex_search_latest.md`
  - `reports/obsidian_rag_vs_vertex_search_latest.json`
- Result summary: Vertex average keyword coverage 0.867 vs Obsidian 0.700. Vertex was stronger on broad subsidy/transport-risk questions and generated citation-style summaries. Existing Obsidian RAG was strongest for precise local asset notes, especially the machine-tool resale query.
- Next improvement before production wiring: deduplicate Vertex results by canonical topic/source path because repeated Auto Research notes can dominate results.

### Hybrid RAG Wiring
- Added `api/vertex_agent_search.py` as an optional supplementary Vertex AI Search client.
- Wired `api/main.py` `next_chat_rag` so existing local Obsidian RAG remains primary and Vertex AI Search context is appended only for lease-domain RAG turns.
- Added response/debug metadata under `vertex_ai_search` and `memory_debug.vertex_ai_search`.
- Added `tests/test_vertex_agent_search.py`; verified disabled mode and Auto Research deduplication.
- Fast rollback: set `VERTEX_AGENT_SEARCH_ENABLED=false`.

### Hybrid RAG Debug Verification
- Ran real `/api/chat` with `debug_memory=true` against a temporary local FastAPI instance on port 8010.
- Initial request exposed two unrelated `/api/chat` runtime bugs:
  - missing `_lease_news_brief_to_dict`
  - missing `_CHAT_JUDGMENT_ASSET_TRIGGERS` / `_CHAT_JUDGMENT_ASSET_ACTIONS`
- Fixed both in `api/main.py`.
- Verified the chat returned 200 OK and `memory_debug.vertex_ai_search.used=true`, `status=ok`.
- Vertex refs returned:
  - `Projects/tune_lease_55/Research/補助金　総合レポート.md`
  - `Projects/tune_lease_55/Research/補助金　対象要件と活用条件の整理_report.md`
  - `Projects/tune_lease_55/Research/補助金制度の全体像調査_report.md`

### Obsidian to Vertex Upload Automation
- Added `scripts/sync_obsidian_to_vertex_agent_search.py`.
- The script exports selected lease knowledge notes from the normal iCloud Obsidian Vault, syncs them to `gs://tune-lease-55-data/agent-search/lease-knowledge/`, and can trigger Discovery Engine `documents:import`.
- Ran dry-run successfully, then ran real sync/import without stale GCS deletion.
- Import operation completed successfully:
  - Operation: `projects/1020894094172/locations/global/collections/default_collection/dataStores/tune-lease-55-obsidian-lease/branches/0/operations/import-documents-18200604050144976794`
  - `successCount=119`
  - `totalCount=119`
- Verified query found the newly uploaded `Projects/tune_lease_55/Research/Auto Research/2026-08-02_asset-operation.md` note in Vertex AI Search.
- Wired the sync into the nightly post pipeline via `scripts/run_daily_improvement_post.sh`.
- Added export-signature state at `data/agent_search/vertex_obsidian_sync_state.json`; unchanged Obsidian corpus now skips GCS sync/import to avoid unnecessary Vertex work and cost.
- Pipeline step log name: `sync_obsidian_to_vertex_agent_search`.

### Vertex Pre-Upload Corpus Gate
- Hardened `scripts/export_obsidian_for_agent_search.py` before any "upload all Obsidian" expansion.
- Added `canonical_topic`, `source_bucket`, and `quality_score` metadata to exported Vertex documents.
- Added canonical-topic deduplication before upload; real export now rejects 47 duplicate-topic notes.
- Added explicit non-public/private markers (`public: false`, `vertex_exclude`, `visibility: private`, confidential markers) and excluded identity/ops/raw material such as Mana logs, hackathon plans, Claude/CSS notes, Judgment Asset OS docs, and inbox material.
- Added a quality gate so thin keyword-only notes do not enter Vertex AI Search.
- Real local export after the gate produced 59 documents; `sync_obsidian_to_vertex_agent_search.py --dry-run` passed and marked the signature changed without cloud upload.

### Vertex Practical Knowledge Workflows
- Added `api/vertex_knowledge_workflows.py` and `/api/vertex-search/workflow`.
- Vertex now has three explicit practical modes:
  - `evidence_support`: source-backed hints for screening comments.
  - `judgment_candidates`: confirmation-question / approval-condition / counterpoint candidates without auto-promotion.
  - `knowledge_audit`: topic duplication, weak coverage, stale areas, and next notes to curate.
- The workflow response always states that Vertex is a supplementary index and Local RAG / Obsidian source notes / human review remain the primary judgment path.
- Added tests in `tests/test_vertex_knowledge_workflows.py`; verified py_compile, related pytest 15 passed, preflight warning-free, and frontend typecheck OK.

### Vertex Workflow Result Obsidian Capture
- Added `save_to_obsidian` support to `/api/vertex-search/workflow`.
- When enabled, Vertex workflow results are saved to the normal iCloud Obsidian Vault under `Projects/tune_lease_55/Research/Vertex Distilled/`.
- Saved notes include workflow mode, query, Answer API summary, high-signal search hits, refs, next actions, grounding metadata, and `review_status: needs_human_review`.
- Capture is deduplicated by workflow mode + query and does not auto-promote into judgment assets.
- Added workflow-result capture tests in `tests/test_vertex_distillation.py`; verified related pytest 30 passed and preflight warning-free.

### Vertex Material Collection Run
- Added `scripts/collect_vertex_workflow_materials.py` for bounded Vertex material collection while credits are available.
- Default topics focus on reusable lease-screening material: subsidy machine tools, re-lease useful life/residual value, resale liquidity, cashflow warning signs, transport risk, bank support, contract/ownership/fraud, and asset operation/maintenance.
- Ran the collector against 8 topics x 3 modes (`evidence_support`, `judgment_candidates`, `knowledge_audit`), page size 5.
- Result: 24 Obsidian notes saved, 0 duplicates, 0 skipped, 0 errors.
- Saved under the normal iCloud Vault: `Projects/tune_lease_55/Research/Vertex Distilled/`.
- Run report: `reports/vertex_workflow_materials_latest.json`.

### Research Mode + Vertex Mining Integration
- Merged chat external Research mode with Vertex material mining.
- When external research is explicitly allowed, `run_external_research_for_chat` still creates the normal Auto Research note, then best-effort captures Vertex workflow materials for the same topic.
- Paired Vertex modes are `evidence_support` and `judgment_candidates`; `knowledge_audit` remains a separate batch/audit mode to avoid heavy per-chat overhead.
- Vertex capture refs are appended into the external research prompt context as `Vertex採掘材料`.
- Added `VERTEX_RESEARCH_MATERIAL_CAPTURE_ENABLED=false` kill switch.
- Verified `py_compile`, related pytest 30 passed, and preflight warning-free.

### Screening Shion Review Vertex Assist
- Wired the screening analysis Shion review prompt to include `【Vertex補助検索ヒント】` so `/api/chat` can use a compact Vertex query instead of the full long review prompt.
- Added backend extraction `_extract_vertex_search_hint` in `api/main.py`; Vertex AI Search now uses the hint when present.
- Added frontend display chips on the screening review card for Vertex ON/status and top Vertex refs.
- Added a guard so screening review prompts and Vertex hint markers are not captured as manual judgment asset candidates.
- Verified real `/api/chat` with a screening-style prompt returned `memory_debug.vertex_ai_search.used=true`, `status=ok`, with subsidy-related Vertex refs.

### Vertex Answer API Grounding Support Score
- Added Answer API support to the hybrid Vertex path for screening review prompts with `groundingSpec.includeGroundingSupports=true`.
- Switched Answer API calls to Discovery Engine `v1beta`; official docs say this is where `groundingScore` / `groundingSupports` are returned when supported.
- Current Obsidian Data Store returned citations but no official `groundingSupports`, so the app now falls back to a citation-span coverage score and labels it with `grounding_score_source="citation_coverage_fallback"`.
- Verified `/api/chat` with `debug_memory=true`: `memory_debug.vertex_answer_api.used=true`, `status=ok`, `grounding_score=0.887`, `support_count=25`, `low_support_claim_count=0`.
- Removed `codex_screening_answer_debug` test rows from local JSONL logs after verification.

### Architecture Simplification Phase
- Started the lightweight architecture cleanup after User noted the system had become complex.
- Added `api/chat_retrieval.py` as the single retrieval assembly module for `/api/chat`.
- Moved local RAG collection, markdown fallback, Vertex AI Search, Vertex Answer API, and Vertex hint extraction out of `api/main.py`.
- Kept behavior stable: `api/main.py` still receives `rag_context`, `rag_refs`, `rag_knowledge_refs`, `vertex_ai_search`, and `vertex_answer_api` in the same shapes as before.
- Added `tests/test_chat_retrieval.py` for Vertex hint extraction and stable zero-RAG result shape.
- Continued the cleanup with three thin helper modules:
  - `api/chat_debug_metadata.py` for Vertex/debug payload shapes.
  - `api/chat_prompt_blocks.py` for prompt block joining and optional PDCA appends.
  - `api/chat_response_pipeline.py` for prompt feedback snapshots and response payload assembly.
- Added `tests/test_chat_architecture_helpers.py` to lock those helper shapes before further extraction.
- Added `api/chat_context_builder.py` for step 4 of the architecture cleanup.
- `ChatContextState` now owns chat category, context mode/budget, response-mode suppression, identity/user memory blocks, continuity/tone blocks, experience loop payloads, grey judgment payloads, and business-plan consult block.
- `api/main.py` now builds that state once and expands it, instead of doing the entire context setup inline.
- Added `tests/test_chat_context_builder.py` for basic lease-question promotion, general response-mode suppression, and news/daily budget suppression.
- Step 5-a completed: added `api/chat_side_effects.py` for chat logging side-effect payload builders.
- Moved `_record_cloudrun_chat_exchange`, prompt-feedback `extra`, memory-usage `extra`, and chat auto-save gating payload logic out of inline `/api/chat` blocks without changing side-effect execution order.
- Added `tests/test_chat_side_effects.py` to lock metadata/extra shapes for general and RAG chat paths.
- Verified: `py_compile` OK; targeted pytest `25 passed`; frontend `npm run typecheck` OK.
- Step 5-b completed: moved Vertex response `extra` construction into `api/chat_response_pipeline.vertex_retrieval_response_extra`.
- Added `api/chat_debug_metadata.append_chat_debug_metadata` so `debug_memory=true` user-memory and Vertex debug enrichment are one stable helper call.
- Updated helper tests; verified again: `py_compile` OK; targeted pytest `27 passed`; frontend `npm run typecheck` OK.
- Step 6 completed: triaged preflight warnings after architecture helper split.
- Fixed undefined-name warnings in `api/main.py` by importing existing Vault research helpers, Cloud Run return DB helpers, and Gemini API key helper from their owning modules instead of duplicating logic.
- Preflight warning count dropped from 57 to 27; remaining warnings are unused imports / compatibility re-exports only.
- Verified: `python3 -m py_compile api/main.py` OK; targeted pytest `27 passed`.
- Step 7 completed: removed clearly unused imports from `api/main.py` while preserving compatibility re-exports used by older tests.
- Preflight warning count dropped from 27 to 10. Remaining import warnings are compatibility re-exports (`improvement`, `vault_hub`, `feedback_loop`); the extra circuit-breaker warning is caused by repeated warn-only preflight runs with those intentional warnings still present.
- Verified: `python3 -m py_compile api/main.py` OK; expanded targeted pytest `46 passed`.
- Step 8 completed: replaced direct compatibility re-export imports in `api/main.py` with `__getattr__` lazy exports for old tests/callers.
- Preserved backward compatibility for old `api.main` references while removing pyflakes unused-import warnings.
- Preflight now reports no warnings.
- Verified: `python3 -m py_compile api/main.py` OK; compatibility tests `23 passed`; expanded targeted pytest `46 passed`; frontend `npm run typecheck` OK.
- Step 9 completed: extracted chat external-research consent/context helpers into `api/chat_external_research.py`.
- Kept thin backward-compatible wrapper functions in `api/main.py` so preflight function-loss guard stays clean and old internal callers remain safe.
- Added `tests/test_chat_external_research.py` for explicit research consent, topic cleanup, general-mode suppression, and permission wording.
- Verified: `python3 -m py_compile api/main.py api/chat_external_research.py` OK; preflight no warnings; expanded targeted pytest `50 passed`; frontend `npm run typecheck` OK before this backend-only wrapper adjustment.
- Step 10 completed: extracted chat classification and context-budget routing into `api/chat_routing.py`.
- Kept old `_is_lightweight_chat_observation`, `_classify_question`, `_chat_context_mode`, and `_chat_context_budget` names in `api/main.py` as thin wrappers for compatibility.
- Added `tests/test_chat_routing.py` to lock lightweight observation, deterministic news classification, context mode, and budget fallback behavior.
- Verified: `python3 -m py_compile api/main.py api/chat_routing.py api/chat_external_research.py` OK; preflight no warnings; expanded targeted pytest `56 passed`; frontend `npm run typecheck` OK.
- Step 11 completed: moved PDCA routing, chat mode instruction text, and basic lease QA chat wrapper into `api/chat_routing.py`.
- Kept old `_should_apply_chat_pdca`, `_chat_mode_instruction`, and `_build_chat_basic_lease_question_context` names in `api/main.py` as thin wrappers.
- Expanded `tests/test_chat_routing.py` to cover PDCA exclusion, mode instruction fallback, and shared basic lease QA block use.
- Verified: `python3 -m py_compile api/main.py api/chat_routing.py api/chat_external_research.py` OK; preflight no warnings; expanded targeted pytest `64 passed`; frontend `npm run typecheck` OK.
- Step 12 completed: extracted lease-intelligence knowledge connection state helpers into `api/lease_intelligence_connection.py`.
- Kept `_count_markdown_notes`, `_build_lease_intelligence_knowledge_connection`, and `_lease_intelligence_indexed_notes_for_compat` in `api/main.py` as thin wrappers.
- Added `tests/test_lease_intelligence_connection.py`; made tests independent from local `OBSIDIAN_VAULT` env.
- Verified: `python3 -m py_compile api/main.py api/lease_intelligence_connection.py api/chat_routing.py` OK; preflight no warnings; expanded targeted pytest `68 passed`; frontend `npm run typecheck` OK before this backend-only test env fix.
- Step 13 completed: moved Markdown fallback RAG root/ref/search helpers into `api/chat_retrieval.py`.
- `build_chat_retrieval_context` now owns default Markdown fallback search via `obsidian_vault_path`; `api/main.py` no longer injects `_search_chat_vault_markdown_fallback` into retrieval assembly.
- Kept `_chat_memory_roots`, `_display_vault_ref`, and `_search_chat_vault_markdown_fallback` in `api/main.py` as thin compatibility wrappers.
- Expanded `tests/test_chat_retrieval.py` to cover roots de-duplication, Obsidian wikilink refs, and legacy fallback hit shape.
- Verified: `python3 -m py_compile api/main.py api/chat_retrieval.py api/lease_intelligence_connection.py api/chat_routing.py` OK; preflight no warnings; expanded targeted pytest `71 passed`; frontend `npm run typecheck` OK.
- Step 14 completed: moved chat response-mode instruction text into `api/chat_routing.py`.
- Kept `_chat_response_mode_instruction` in `api/main.py` as a thin compatibility wrapper.
- Expanded `tests/test_chat_routing.py` to cover general and Shion response-mode instruction text.
- Verified: `python3 -m py_compile api/main.py api/chat_routing.py api/chat_retrieval.py api/lease_intelligence_connection.py` OK; preflight no warnings; expanded targeted pytest `72 passed`; frontend `npm run typecheck` OK.
- Step 15 completed: moved `memory_debug` response payload assembly and Relationship Loop Engineering debug payload into `api/chat_debug_metadata.py`.
- Kept `_chat_memory_debug_payload` and `_build_relationship_loop_engineering_payload` in `api/main.py` as thin compatibility wrappers.
- Expanded `tests/test_chat_architecture_helpers.py` to cover memory debug payload limits and relationship-loop evidence shape.
- Verified: `python3 -m py_compile api/main.py api/chat_debug_metadata.py api/chat_retrieval.py api/chat_routing.py api/lease_intelligence_connection.py` OK; preflight no warnings; expanded targeted pytest `74 passed`; frontend `npm run typecheck` OK.
- Step 16 completed: moved lease-intelligence dialogue shared-memory public payload shaping into `api/chat_debug_metadata.py`.
- Kept `_dialogue_shared_memory_public_payload` in `api/main.py` as a thin compatibility wrapper; did not move shared-memory context construction because it depends on multiple memory side-effect-adjacent helpers.
- Expanded `tests/test_chat_architecture_helpers.py` to cover compact shared-memory public payload shape and ref limits.
- Verified: `python3 -m py_compile api/main.py api/chat_debug_metadata.py api/chat_retrieval.py api/chat_routing.py api/lease_intelligence_connection.py` OK; preflight no warnings; expanded targeted pytest `75 passed`; frontend `npm run typecheck` OK.
- Step 17 completed: extracted chat identity memory loading into `api/chat_identity_memory.py`.
- Moved identity memory constants, Markdown file reading, H2 section extraction, layer/fallback-pack assembly, and cache into the new module.
- Kept `_read_chat_memory_file`, `_extract_markdown_section`, `_load_chat_identity_memory_payload`, and `_build_chat_identity_memory_prompt_block` in `api/main.py` as thin compatibility wrappers.
- Added `tests/test_chat_identity_memory.py` for file clipping, section extraction, layer files, fallback latest pack, and prompt block prefix.
- Verified: `python3 -m py_compile api/main.py api/chat_identity_memory.py api/chat_debug_metadata.py api/chat_retrieval.py api/chat_routing.py api/lease_intelligence_connection.py` OK; preflight no warnings; expanded targeted pytest `80 passed`; frontend `npm run typecheck` OK.
- Step 18 completed: extracted user personal-memory read/prompt loading into `api/chat_user_personal_memory.py`.
- Moved personal-memory keyword filtering, Cloud Run event restoration via injected reader, prompt block assembly, cache, and cache invalidation into the new helper.
- Kept `_read_personal_memory_lines`, `_read_cloudrun_personal_memory_lines`, `_load_user_personal_memory_payload`, and `_build_user_personal_memory_prompt_block` in `api/main.py` as thin compatibility wrappers; kept capture/write side effect in `api/main.py` and only delegates cache invalidation.
- Added `tests/test_chat_user_personal_memory.py` for keyword filtering, all-lines local memory, injected Cloud Run memory restoration, dedupe/priority, and prompt block prefix.
- Verified: `python3 -m py_compile api/main.py api/chat_user_personal_memory.py api/chat_identity_memory.py api/chat_debug_metadata.py api/chat_retrieval.py api/chat_routing.py api/lease_intelligence_connection.py` OK; preflight no warnings; targeted pytest `22 passed`; frontend `npm run typecheck` OK from `frontend/`.
- Step 19 completed: extracted chat judgment-asset capture helpers into `api/chat_judgment_asset_capture.py`.
- Moved autoresearch candidate loading/state overlay, manual candidate creation, chat teaching-claim extraction, candidate type classification, and duplicate-aware capture orchestration into the helper.
- Kept `_load_autoresearch_judgment_asset_candidates`, `_create_manual_judgment_asset_candidate`, `_extract_chat_judgment_asset_claim`, `_chat_judgment_asset_candidate_type`, and `_capture_chat_judgment_asset_if_needed` in `api/main.py` as thin compatibility wrappers.
- Added `tests/test_chat_judgment_asset_capture_helpers.py`; existing `tests/test_chat_judgment_asset_capture.py` still passes through `api.main` wrappers.
- Verified: `python3 -m py_compile api/main.py api/chat_judgment_asset_capture.py` OK; `pytest -q tests/test_chat_judgment_asset_capture.py tests/test_chat_judgment_asset_capture_helpers.py tests/test_chat_architecture_helpers.py` -> `16 passed`; preflight no warnings; frontend `npm run typecheck` OK from `frontend/`.
- Step 20 completed: extracted chat language feedback side-effect helpers into `api/chat_language_feedback.py`.
- Moved raw language-material preservation, deterministic response-impact scoring, and response-impact JSONL/writeback recording into the helper; `api/main.py` now only injects paths, redaction, and Cloud Run writeback.
- Kept `_capture_language_judgment_material`, `_score_response_impact`, and `_record_response_impact_prediction` in `api/main.py` as thin compatibility wrappers.
- Added `tests/test_chat_language_feedback.py` for material JSONL preservation/redaction, empty-after-redaction handling, high-risk response scoring, and response-impact shadow entry writing.
- Verified: `python3 -m py_compile api/main.py api/chat_language_feedback.py api/chat_judgment_asset_capture.py` OK; targeted pytest `20 passed`; preflight no warnings; frontend `npm run typecheck` OK from `frontend/`.
- Step 21 completed: extracted human response feedback prompt helpers into `api/chat_human_feedback.py`.
- Moved chat-side human response feedback JSONL loading, route/default matching summary, and Shion light-tone feedback prompt generation into the helper.
- Kept `_load_human_response_feedback`, `_summarize_human_response_feedback`, and `_build_shion_light_tone_feedback_prompt_block` in `api/main.py` as thin compatibility wrappers; did not touch router feedback-loop persistence.
- Added `tests/test_chat_human_feedback.py` for bad-JSON skipping, limit behavior, route/default feedback summary, recent comments, and light-tone trigger/skip behavior.
- Verified: `python3 -m py_compile api/main.py api/chat_human_feedback.py api/chat_language_feedback.py api/chat_judgment_asset_capture.py` OK; targeted pytest `22 passed`; preflight no warnings; frontend `npm run typecheck` OK from `frontend/`.
- Step 22 completed: extracted Shion persona prompt helpers into `api/chat_persona_prompts.py`.
- Moved non-domain short-input prompt, self-reference/specificity prompt, and deterministic human-device resonance prompt into the helper.
- Kept `_build_shion_non_domain_prompt_block`, `_build_shion_specificity_prompt_block`, and `_build_shion_human_device_resonance_prompt_block` in `api/main.py` as thin compatibility wrappers.
- Added `tests/test_chat_persona_prompts.py`; existing `tests/test_shion_specificity_prompt.py` still passes through `api.main` wrappers.
- Verified: `python3 -m py_compile api/main.py api/chat_persona_prompts.py api/chat_human_feedback.py api/chat_language_feedback.py api/chat_judgment_asset_capture.py` OK; targeted pytest `25 passed` then focused wrapper pytest `19 passed`; preflight no warnings; frontend `npm run typecheck` OK from `frontend/`.
- Step 23 completed: extracted screening experience promotion helpers into `api/screening_experience_promotion.py`.
- Reused `api.routers.feedback_loop` screening-experience DB helper implementations through thin `api/main.py` wrappers, and moved closed case-result to screening-experience payload construction into the helper.
- Added `tests/test_screening_experience_promotion.py`; verified related screening-experience tests pass and preflight has no warnings.
- Step 24 completed: extracted Cloud Run pending-case normalization helpers into `api/cloudrun_pending_cases.py`.
- Moved Cloud Run score/event case-id parsing, JSON dict loading, pending item rendering, and GCS-event pending filtering into the helper.
- Added `tests/test_cloudrun_pending_cases.py`; verified pending-case related tests pass and preflight has no warnings.
- Step 25 completed: extracted reflection prompt helpers into `api/chat_reflection_prompts.py`.
- Moved Shion judgment response shape prompt, reflection gate prompt, and consciousness UX prompt into the helper; kept `api/main.py` wrappers.
- Added `tests/test_chat_reflection_prompts.py`; verified related prompt tests pass and preflight has no warnings.
- Step 26 completed: extracted continuity prompt helpers into `api/chat_continuity_prompts.py`.
- Moved route/label detection, explicit continuation detection, delta awareness, memory-to-judgment, and memory expression prompt helpers into the module; kept `api/main.py` wrappers.
- Added `tests/test_chat_continuity_prompts.py`; verified continuity/naturalness tests pass and preflight has no warnings.
- Step 27 completed: extracted grey judgment memory prompt helpers into `api/chat_grey_judgment.py`.
- Moved qualitative case summary, gunshi judgment memory filtering, and grey judgment prompt assembly into the helper with injected case/judgment loaders.
- Added `tests/test_chat_grey_judgment.py`; verified grey judgment/continuity prompt tests pass and preflight has no warnings.
- Step 28 completed: extracted lease-news summary rendering helpers into `api/lease_news_summary_render.py`.
- Moved safe news filename, code/phrase normalization, and rendered summary/usage memo assembly into the helper; kept Gemini fetch/save side effects in `api/main.py`.
- Added `tests/test_lease_news_summary_render.py`; verified rendering/dialogue/continuity prompt tests pass, preflight has no warnings, and frontend `npm run typecheck` OK from `frontend/`.
- Step 29 completed: extracted `SecurityHeadersMiddleware` into `api/security_headers.py`.
- Step 30 completed: extracted dialogue history/input bounding helpers into `api/dialogue_history_utils.py`.
- Moved long dialogue input detection, history text clipping, and compacting/budgeting into the helper; added `tests/test_dialogue_history_utils.py`.
- Step 31 completed: ran final architecture cleanup verification over the newly extracted helper set.
- Step 32 decision: kept endpoint orchestrators in `api/main.py` intentionally: `/api/chat` and `/api/lease-intelligence/dialogue` are now mostly wiring together helpers, but still large because they own request/response branching and side-effect sequencing.
- Verification after final cleanup pass: targeted pytest `43 passed`; `python3 scripts/preflight_pr_guard.py || true` warning-free; frontend `npm run typecheck` OK from `frontend/`.
- Step 33 completed: extended lease-news cleanup by moving Obsidian note Markdown rendering, recent-news frontmatter/body parsing, summary-line cleanup, and duplicate-key generation into `api/lease_news_summary_render.py`.
- Added regression coverage for rendered news note frontmatter, recent note parsing, source URL dedupe, and noisy summary-line removal.
- Step 34 completed: extracted debate-review Obsidian Markdown generation into `api/debate_note_render.py`; `save_debate_to_obsidian` now only validates Vault, creates `Debates/`, writes the helper-rendered content, and returns the relative path.
- Added `tests/test_debate_note_render.py` and kept `tests/test_debate_save_to_obsidian.py` passing through the endpoint wrapper.
- Step 35 final architecture cleanup audit: `api/main.py` is now 8161 lines; remaining >=90-line functions are primarily lifespan, scoring/register endpoints, improvement-log orchestration, `/api/chat`, and `/api/lease-intelligence/dialogue`. These still coordinate DB, filesystem, GCS, LLM, and response sequencing, so they were intentionally left rather than splitting high-risk orchestration late in the cleanup.
- Verification after final pass: `python3 -m py_compile` over `api/main.py` plus all new helper modules OK; targeted pytest `50 passed`; `python3 scripts/preflight_pr_guard.py || true` warning-free; frontend `npm run typecheck` OK from `frontend/`.
- Stability check after `8a12e38`: Next `/home` and FastAPI `/docs` were healthy, but `/api/system/cloud-status` returned 500 because `api/routers/system_misc.py` referenced `_LEASE_DB_PATH` without defining it in that router. Fixed by using `runtime_paths.get_db_path()`, adding module logger/json imports, and adding `tests/test_system_misc_cloud_status.py`.
- Stability verification: `cloud-status` now returns 200 with `ready=true`; `/healthz`, `/api/master/assets`, `/api/lease-news/recent`, and real `/api/chat` with `debug_memory=true` returned successfully. Targeted pytest `58 passed`, focused system tests `25 passed`, preflight warning-free, frontend typecheck OK.
