# 2026-06-07

## Snapshot
- Context: the Next/FastAPI/Cloudflare stack had recurring connection loss.
- Action: moved the launcher into a LaunchAgent, added repeatable install/start scripts, and tightened restart workflow guidance.
- Result: the stack stayed up independently and the quick tunnel remained available after restart.
- Next: continue using LaunchAgent-based supervised startup instead of foreground sessions.

## Persistent Next/FastAPI/Cloudflare launcher
- User reported recurring connection loss. Logs showed API and Next stopped together because the foreground `run_next_stable.sh` launcher received termination when its Codex/terminal session ended.
- Installed `com.tunelease.next` as a macOS LaunchAgent with `RunAtLoad` and `KeepAlive`, so the stack now runs independently and restarts automatically.
- Added `scripts/install_next_launchagent.sh` for repeatable installation and startup.
- Updated `.agents/skills/restart-next-cloudflare/SKILL.md` to prefer `launchctl kickstart -k` instead of a foreground launcher.
- Verification: LaunchAgent state `running`; local API `/docs`, local Next `/home`, and Cloudflare `/home` all returned HTTP 200.
- Current quick tunnel URL after migration: `https://neon-convenience-itself-red.trycloudflare.com/home`.
- Remaining limitation: account-less Cloudflare Quick Tunnel URLs can still change. A named tunnel is required for a permanent public URL.

## Promotable Items
- The launcher should remain LaunchAgent-first; foreground restart workflows are too fragile for routine use.
- Cloudflare quick tunnel URLs are disposable and must be refreshed from the newest log when debugging.

## e-Stat machinery orders analysis
- Fetched Cabinet Office machinery orders table `0003355222` from e-Stat for March 2023 through March 2026.
- Saved 518 observations (7 demand categories x original/seasonally adjusted x 37 months) under `data/external/estat_machinery_orders/`.
- Added reproducible script `scripts/fetch_estat_machinery_orders.py`, analysis report `reports/machinery_orders_202303_202603.md`, and chart `reports/machinery_orders_202303_202603.png`.
- March 2026 private-sector orders excluding ships and electric power were 1.011 trillion yen, down 9.4% month-on-month but up 5.9% year-on-year; January-March was up 6.4% quarter-on-quarter.
- Recommended system use: macro/industry outlook overlay and monitoring feature, not a standalone borrower credit decision. Preserve publication dates and data vintages to prevent look-ahead bias.
