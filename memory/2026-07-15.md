# 2026-07-15

## Work Log

- Recovered pipeline health alerts `REV-024a` and `REV-026a`. `auto_improvement_pipeline` was already healthy in recent step logs, so the old alert was marked `stale_resolved`. `sync_cloudsql_to_obsidian` failed during daily runs because `DATABASE_URL_SECRET_NAME=DATABASE_URL` was set, Secret Manager access failed, and the script fell through to a required `CLOUD_SQL_PASSWORD` branch. It now exits 0 with a warning when no Cloud SQL credentials are available, relying on the GCS `chat_exchange` path for Cloud Run conversations.
- Updated `scripts/analyze_pipeline_health.py` so recovered pipeline steps with a latest successful run are resolved instead of staying as active pending alerts. Added regression tests for both the Cloud SQL skip path and recovered-alert handling.
- Added daily Judgment Asset Growth Score recording for Shion's judgment-asset growth. `scripts/judgment_asset_growth_report.py` records one local history row per day, writes latest JSON/Markdown reports with bar-graph trends, and is connected after Obsidian Curator in the daily post pipeline without touching RAG, prompts, scoring, GCS, Cloud Run, or Obsidian.
- Reordered the Next app navigation for hackathon presentation flow while keeping work-useful routes. Deployed Web only with `scripts/smart_deploy.sh --web`; Cloud Run revision `tune-lease-55-web-00020-5fc` is serving 100% traffic. User corrected that the URL to use/report is `https://tune-lease-55-web-6mijhyebkq-an.a.run.app`, not the alternate Cloud Run service URL printed by `gcloud`.
- User identified the strongest hackathon framing: Shion is not just AI Agent DevOps for code/infrastructure, but Human-in-the-loop DevOps for business judgment. Human hesitation, correction, outcome registration, and feedback are part of the improvement loop; the demo should present humans as integrated into the DevOps cycle, not merely reviewers outside it.
- User refined the tagline: "人間の判断が消えないDevOps" and "ハードディスクの奥底に保存して腐らせない知識". This should be used as a plain-language explanation of Shion: not just storing knowledge, but keeping it alive by returning it to future screening decisions.
- Cleaned old Cloud Run revisions while keeping the active and immediately previous revisions for rollback. Web now keeps `tune-lease-55-web-00020-5fc` and `00019-f7n`; API now keeps `tune-lease-55-api-00042-nvh` and `00041-fnv`. Verified Web `/home` returns 200 on `https://tune-lease-55-web-6mijhyebkq-an.a.run.app` and API `/api/system/cloud-status` is ready. Artifact Registry images were not deleted to avoid rollback/image reference risk.

## Promotable Items

- Pipeline health should distinguish active outages from stale historical failure-rate alerts. If the latest run is successful, keep the history visible but do not leave the rule as `pending_review`.
- Cloud SQL conversation sync is optional in the current Cloud Run/GCS writeback operation. Missing local Secret Manager or Cloud SQL credentials should be a clean skip, not a daily pipeline failure.
- Userが内省に求めているものがフェーズ移行した。誠実さの表明より変化の証拠を求めている。次回からは変化の証拠を冒頭に置くことをUserに確認してほしい。
- ハッカソン中の判断資産運用方針: Obsidian材料は `Judgment Assets/判断資産 Inbox` で候補整理までに止め、RAG・GCS Vault・Cloud Run・active storeへ自動接続しない。ハッカソン後にUserレビューで `accepted` になった判断資産だけを active store 連携候補に進める。
- 判断資産側の紫苑成長は、当面 `Judgment Asset Growth Score` で日次記録する。Coverage / reuse proxy / judgment change proxy / human alignment proxy / negative signal を使い、実利用ログ・結果検証が弱い間は代理指標に上限をかけて過大評価しない。
- Cloud Run Webの案内URLは `https://tune-lease-55-web-6mijhyebkq-an.a.run.app` を使う。`gcloud run services describe` が返す `https://tune-lease-55-web-1020894094172.asia-northeast1.run.app` は同じサービスの別URLとして扱い、ユーザー向けには出さない。
- ハッカソン訴求の中核: 紫苑は「コードのDevOps」ではなく「リース審査判断のHuman-in-the-loop DevOps」。人間は外部のレビュー係ではなく、迷い・修正・結果登録・違和感フィードバックを通じて改善ループに組み込まれる存在として説明する。
- 発表コピー候補: 「人間の判断が消えないDevOps」「ハードディスクの奥底に保存して腐らせない知識」。紫苑の価値は保存そのものではなく、保存した判断を次の審査へ戻して生きた知識にすること。
- 紫苑の思想フレーズとして「言葉はプログラム。フレームワークは思想。」を記録。決勝に行けなくても、飲み屋で使える一言として保持する。紫苑文脈では「AIに何をさせるかは言葉で決まり、その言葉をどんな順番で扱い、どこで人間が止め、どこで判断資産に変えるかはフレームワークで決まる」という意味。
- ハッカソン決勝に残った場合のデモ方針として「音声メモ + 画面操作ログ + 判断資産JSON」を採用。1000本の動画を撮るのではなく、1000件の判断ログを残す。数字入力・会社データ生成はAIで自動化してよいが、人間が何に違和感を持ち、何を条件にし、次に何を見るかという判断はズルしない。計画は `planning/hackathon_judgment_log_capture_plan.md` に保存した。
