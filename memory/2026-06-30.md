# 2026-06-30

## Next / Lease Intelligence Dialogue
- `/lease-intelligence` の「対話AIへ接続できませんでした。Gemini APIの状態を確認してください。」は、Gemini API自体ではなく Next rewrites 経由の `/api/lease-intelligence/dialogue` が `socket hang up` するケースが原因だった。
- FastAPI直叩き、Next経由、Cloudflare経由でGemini応答は確認済み。対策として `frontend/src/app/api/lease-intelligence/dialogue/route.ts` を追加し、対話POSTを汎用rewriteではなくNext Route HandlerからFastAPIへ明示プロキシするようにした。
- 今後同様の表示が出たら、まず `logs/next/next_*.log` の `socket hang up` と、FastAPI直叩き `/api/lease-intelligence/dialogue` の成否を分けて見る。

## Cloud Run 運用ルール
- User から明示的に依頼されるまで、Cloud Run デプロイは実行しない。実装・検証・commit/push までは必要に応じて進めても、Cloud Run 反映は確認を取ってから行う。
