# 2026-08-21

## 朝のSlack日次改善レポート未着対応
- 事実: `scripts/send_daily_improvement_slack.py` は正常終了扱いだったが、`.streamlit/secrets.toml` の `SLACK_WEBHOOK_URL` が欠損形状だったため `SLACK_WEBHOOK_URL looks incomplete or invalid; skipping Slack improvement report.` で送信をスキップしていた。
- 影響: `data/pipeline_step_log.jsonl` では `send_daily_improvement_slack` が exit 0 になり、パイプライン成功に見える一方でSlackには投稿されなかった。
- 次の行動: Slack未着時は `~/Library/Logs/tunelease/improvement_YYYYMMDD.log` のSlackスキップ文言と `data/slack_daily_improvement_state.json` の `last_sent_at` を確認する。プロジェクトの `.streamlit/secrets.toml` はgit無視対象なので、実WebhookをローカルLaunchAgentの既存値に揃える。

## 朝ニュース未更新に見える問題の修正
- 事実: 2026-08-21 06:00 のニュース収集自体は成功し、Obsidian の `Projects/tune_lease_55/News/2026-08-21_*` と `data/lease_news_actions_latest.json` は更新されていた。一方でインストール済みLaunchAgentは古い `anaconda3/bin/python` 設定のままで、ホーム画面はニュース欄が既定非表示、当日ブリーフも日付単位の既読キーで再表示されにくかった。
- 影響: データは新しいのに、画面上は「朝ニュースが更新されていない」と見える状態になった。
- 次の行動: `~/Library/LaunchAgents/com.tunelease.lease-news-collector.plist` をリポジトリ版へ同期し、launchdを再ロード済み。ホーム画面はニュース欄を既定表示に戻し、既存ブラウザにも一度だけ表示復帰をかけ、ブリーフ既読キーは日付+内容単位にした。

## LaunchAgent設定の全体同期
- 事実: `~/Library/LaunchAgents/com.tunelease.*.plist` を全件監査し、管理対象9件が古いままだった。追加で `dispatch-log` / `improvement-pipeline` / `morning-rag-review` / `vault-watcher` はリポジトリ正本がなく、古い実行系や秘密値直書きが残っていた。
- 影響: 朝・夜間ジョブが古い時刻、Anaconda直指定、Vault変数不足、plist内秘密値に依存し続けるリスクがあった。
- 次の行動: 未管理4件の正本plistを `launchd/` に追加し、全16件をリポジトリ正本と一致させて再ロード済み。監査結果は Anaconda直指定0件、秘密キー直書き0件。`com.tunelease.next` は正本一致だが未ロードで、外部公開トンネルを起動し得るため自動起動は保留。

## LaunchAgent監査スクリプト追加
- 事実: `scripts/audit_launchagents.py` を追加し、リポジトリ正本と `~/Library/LaunchAgents` の差分、未管理plist、秘密値直書き、Anaconda直指定、launchd未ロードを一発で監査できるようにした。`morning-rag-review` は `obsidian-reindex` と3:00同時起動だったため03:20へ変更した。
- 影響: 今後「朝ニュース/朝報/夜間処理が更新されない」時に、設定ドリフトを手作業で探さず検出できる。Vault系ジョブのスケジュール衝突も既存の `check_obsidian_ops_consistency.py` で検知される。
- 次の行動: 異常時は `python3 scripts/audit_launchagents.py` と `python3 scripts/check_obsidian_ops_consistency.py` を先に実行する。`com.tunelease.next` 未ロード警告は外部公開トンネルを起動し得るため、必要時だけ明示的にロードする。

## Obsidian wikiリンク一括反映
- 事実: 通常の iCloud Obsidian Vault に対して `scripts/auto_wikilink.py` を実行し、`Projects/tune_lease_55` / `リースニュース` / `リース知識` のリンク可能な本文言及を wikilink 化した。実行前に `/private/tmp/obsidian_vault_before_auto_wikilink_20260821` へバックアップを作成した。
- 影響: 908ファイルが更新され、graph effect レポート由来の hub-link 12件と日付ログの前後ナビも反映された。再ドライランは `total changed: 0, errors: 0`。
- 次の行動: リンク不調を疑う場合は `python3 scripts/auto_wikilink.py --dry-run --vault "$OBSIDIAN_VAULT"` と `python3 scripts/monitor_obsidian_environment.py --vault "$OBSIDIAN_VAULT"` を確認する。今回の監視 `warn` は wikilinks ではなく Private Reflection / memory insight sidecar 由来。

## Obsidian孤立ノート追加接続
- 事実: 前回の `auto_wikilink.py` は対象フォルダが狭く、Vault全体の孤立ノートには届いていなかった。`scripts/auto_wikilink.py` を修正し、graph effect レポートに出た対象外フォルダの孤立ノートにも hub-link を付けられるようにした。Daily/ログ/Reflection/Generated は自動接続対象から除外し、ハブ側も Daily/ログ系を除外した。
- 影響: 使用シグナルがある孤立ノート34件に知識ハブへのリンク68件を追加した。`isolated_but_used` は34件から2件まで減少し、残り2件は Reflection / Generated のため意図的に自動接続しない対象。
- 次の行動: まだ見える孤立の多くは `Generated`、会話記録、クリップ、アーカイブ、lease-wiki-vault、作業ログなどで、判断に使われた痕跡が弱い dormant isolate。全部を無理に接続せず、使用シグナルが出たものから接続する。

## 紫苑ADK Obsidian Curator追加
- 事実: `api/shion_obsidian_curator.py` を追加し、紫苑ADKへ読み取り専用の `review_obsidian_vault_health` と `suggest_obsidian_curation_actions` を登録した。`api/shion_agent.py` のツール説明にも、検索で拾えないテーマやObsidian情報健康の棚卸し時に使うよう追記した。
- 影響: 紫苑はVaultを書き換えずに、孤立ノート、リンク、retrieval graph、監視結果、テーマ別の関連リンク/検索語候補を最大件数つきで提案できる。自動反映・Chroma再インデックス・プロンプト変更はしない。
- 次の行動: 検索/チャットで拾えないテーマが出たら `suggest_obsidian_curation_actions(theme)` で候補を見て、人間承認後に少数リンクまたは検索語だけ追加する。`pytest -q tests/test_shion_agent_tools.py` は14件成功。

## 紫苑ADK Obsidian Curator日次診断化
- 事実: `scripts/shion_obsidian_curator_daily.py` を追加し、ADK登録済みのObsidian Curatorツールを毎日読み取り専用で実行して `reports/shion_obsidian_curator_daily_latest.{json,md}` を生成するようにした。`scripts/run_daily_improvement_post.sh` の `build_obsidian_graph_judgment_effect` 後に接続し、Slack日次レポートにも短い `Obsidian Curator` 節を追加した。
- 影響: 毎朝、孤立ノート・degree0・検索で拾えないテーマ候補を最大5件だけ確認できる。Vault書き込み、Chroma再インデックス、プロンプト変更、自動リンク反映はしない。
- 次の行動: Slackの `isolated_used` が増えた時、またはテーマ候補で実際に検索漏れが見えた時だけ、人間承認後に少数リンク/検索語を追加する。`py_compile`、`bash -n scripts/run_daily_improvement_post.sh`、Slack `--dry-run`、`pytest -q tests/test_shion_agent_tools.py` は成功。
