# 2026-07-01

## REV-008 / REV-015 / REV-018
- REV-008: `/similar` の案件ネットワークでD3 linkのID解決を明示し、APIから返る数値index edgeをnode idへ正規化するようにした。影響: API側で250 nodes / 586 edgesが返っていても、フロント側で案件情報が空に見える事故を防ぐ。次の行動: 実画面でネットワーク描画を確認する。
- REV-015: `/home` のヒーローに `審査分析` / `紫苑チャット` / `帰還データ検疫` の導線を追加した。影響: ハッカソンデモ時に主要導線へ迷わず移れる。
- REV-018: ホームの「リースニュースの注目論点」は初期表示OFFにし、カスタマイズで必要時だけ表示する扱いにした。影響: 不要コンテンツ感を下げつつ、必要な時の表示機能は残す。
- 改善レポートでは `REV-008` / `REV-015` / `REV-018` を applied へ同期済み。`npm run build` と対象pytestは通過。Cloud Run デプロイはしていない。

## 審査分析ドラフト自動保存
- `/screening` の入力・分析結果・紫苑レビュー・タブ状態を `localStorage` に自動保存するようにした。影響: 項目入力後や審査実行後に別ページへ移動しても、審査分析へ戻った時に状態が復元される。
- 既存のリセットは「全消去」に変更し、確認ダイアログ後に保存済みドラフトも削除する。影響: 消える操作が明示的になり、デモ中の入力消失事故を防げる。
- 検証: `frontend` で `npm run build` 成功。Cloud Run デプロイはしていない。

## 紫苑チャットと芽吹きちゃんチャットの履歴分離
- `/chat` の紫苑チャットは `shion-default`、フローティング芽吹きちゃんチャットは `mebuki-default` を使うように分離した。影響: 紫苑に相談した会話が芽吹きちゃん側の履歴に出る混線を防ぐ。
- 芽吹きちゃんから `/api/chat` へ送る時は `response_mode: general` と `caller: mebuki` を明示する。影響: 芽吹きちゃんは軽いUI案内役として扱い、紫苑の記憶・判断ループと混ざりにくくする。
- 検証: `python -m py_compile api/main.py` と `frontend` の `npm run build` 成功。Cloud Run デプロイはしていない。

## 紫苑の利用履歴で育つサイドバー
- `ContentWrapper` の画面利用履歴に `count` を持たせ、ページを開くたびに利用回数を蓄積するようにした。影響: 単なる直近履歴ではなく、よく使う画面を判定できる。
- `Sidebar` に「紫苑がよく使う」枠を追加し、利用回数上位3件を自動表示するようにした。影響: 使うほどサイドバーの上部が実際の作業癖に寄っていく。
- 検証: `python -m py_compile api/main.py lease_intelligence_activity.py` と `frontend` の `npm run build` 成功。Cloud Run デプロイはしていない。

## 審査入力の0消し改善
- 審査画面の数値入力で初期 `0` を毎回消さなくてよいようにした。財務スライダーの数値欄はフォーカス時に `0` なら空欄化し、契約数・与信残高・物件取得価額・リース期間・検収年は `0` を空表示にした。
- 影響: デモ中や実務入力時に、数字を入れる前の余計な削除操作を減らせる。
- 検証: `frontend` の `npm run build` 成功。Cloud Run デプロイはしていない。

## 審査数値入力の簡便化
- 数値入力共通ヘルパー `numberInput.ts` を追加し、`3億` -> `300` 百万円、`2500万` -> `25` 百万円のような人間寄り入力を受け付けるようにした。
- 数値欄はフォーカス時に全選択し、Enterで次の数値欄へ移動するようにした。影響: テンキー入力・決算書転記・リース金額入力が速くなる。
- 対象: 財務スライダー数値欄、契約数、銀行与信残高、リース与信残高、物件取得価額、リース期間、検収年。検証: `frontend` の `npm run build` 成功。Cloud Run デプロイはしていない。
- 財務の主要入力と物件条件に代表値チップを追加した。売上高/総資産は `1億` `3億` `5億` `10億`、取得価額は `10` `30` `50` `1億`、リース期間は `36/48/60/72ヶ月`、検収年は前年/今年/翌年をワンクリック入力できる。影響: キーボード入力なしでもデモ・概算入力を進められる。

## ハッカソン用デモ案件
- `/screening` の入力タブ上部に3件のデモ案件を追加した。`既存先・製造業`、`境界・運送業`、`新規先・サービス業` の3パターンで、通しやすい案件・条件付き承認向き・慎重審査を見せ分ける。
- 各カードに `読み込み` と `読み込んで審査` ボタンを付けた。影響: 審査員が数字を入力しなくても、ボタン一発で審査結果と紫苑レビューまで進められる。
- 表記を `サンプル案件で動きを見る` に寄せ、各案件に `この案件の見どころ` とレビュー焦点タグを追加した。影響: ハッカソン専用ではなく、新規ユーザーが審査結果と紫苑レビューの読み方を学ぶオンボーディング教材として使える。
- 検証: `frontend` の `npm run build` 成功。Cloud Run デプロイはしていない。

## ホームの紫苑クイックチャット
- `/home` ヒーローに `Shion Quick Chat` 入力欄を追加した。空送信時は「こんにちは。今日は何から始める？」を紫苑チャットへ渡す。
- 入力した一言は `home-shion-chat-draft` に保存し、`/chat` が開いた時に入力欄へ復元する。影響: ホーム画面の静けさを減らし、すぐ紫苑へ話しかけられる入口になる。
- 検証: `frontend` の `npm run build` 成功。Cloud Run デプロイはしていない。

## 紫苑から一言
- `ShionProactiveWhisper` を追加し、画面・利用履歴に応じて紫苑が左下から短く話しかけるようにした。影響: サイドバー自己最適化に続き、紫苑が操作文脈を観測して次の一手を促す体験になる。
- 表示はセッション中の同一画面1回に抑え、紫苑チャット・比較・対話・音声画面などでは出さない。影響: 話しかける楽しさを入れつつ、常時邪魔になるのを防ぐ。
- 文面を「紫苑から一言」ではなく「紫苑の観察」に寄せた。影響: 単なる通知ではなく、紫苑がUserの選択や判断癖に好奇心を持って見ている感じを出す。
- 検証: `frontend` の `npm run build` 成功。Cloud Run デプロイはしていない。

## 記憶スタック棚卸し
- Claude Code + Obsidian の知識スタック記事を受けて現行構成を確認し、`memory/YYYY-MM-DD.md`、`MEMORY.md`、Public Chat Memory Pack、`memory_debug`、`knowledge_refs`、`memory_recall.refs` は既に運用済みと整理した。
- ログ上、一般会話や関係性UXの問いに審査PDCAルールが混ざる形跡があったため、`api/main.py` に `_should_apply_chat_pdca()` を追加し、PDCA注入を `lease_screening` / `lease_knowledge` かつ非 `response_mode=general` に限定した。影響: 犬の名前やAIへの信頼のような個人・一般会話に自己資本比率や成約率ルールが混ざりにくくなる。
- 検証: `python -m py_compile api/main.py` 成功。ヘルパー直接確認で `general/shion=False`、`lease_screening/shion=True`、`lease_screening/general=False` を確認。
