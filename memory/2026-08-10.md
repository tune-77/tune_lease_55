# 2026-08-10

- 改善PMレポートで、前日にUI削除/解決した候補が翌朝 `needs_review` として再掲される問題を修正。原因は `ledger.jsonl` 読み込みが単純な最終行優先で、日次パイプラインの自動 `needs_review` が人間の `deleted/applied/parked` 判断を上書きしていたこと。`api/main.py` と `sync_improvement_reports.py` で terminal status を優先し、`apply_failed` は古い `applied` を取り消す状態として扱うようにした。2026-08-10の改善レポートは `REV-292` のみ要レビュー、`REV-294` は保留、`REV-291` は削除済みとして再掲抑制。
