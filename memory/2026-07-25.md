# 2026-07-25

- User clarified a core judgment-asset principle: even if cases are demo/synthetic, once User actually scores, reasons, conditions, and ringi wording from a field-practitioner perspective, that output is current field judgment asset. The important distinction is not demo vs real case, but whether it captures User's actual on-site screening judgment. Impact: Presentation and reports should avoid dismissing demo-labeled cases as non-assets; instead separate `human_judgment_asset` from `outcome_validated_case`. Next action: label the 500-case exercise as human-labeled field judgment assets, while keeping actual delinquency/outcome validation as a later layer.
- User identified why the 500-case idea is unusually strong: the labels are not outsourced annotation or generic AI-generated answers, but the developer's own field knowledge as a lease-screening practitioner. Impact: in the pitch, emphasize "developer/operator's actual judgment embedded into the system" rather than only "synthetic demo data." Next action: frame Shion as a system where the builder's domain judgment becomes reusable operational software.
- User extended the 1000-case idea into an operating model: first create a baseline judgment asset corpus from the developer/practitioner's own 1000 judgments, then let other people build on top of it with their own judgments, corrections, and outcomes. Impact: Shion is not just a personal assistant; it can become a shared judgment-asset platform where an initial expert baseline is continuously extended. Next action: pitch this as "founder field judgment baseline -> team accumulation -> result validation -> reusable institutional knowledge."
- User said Claude/Codex work logs are also now saved to the Vault. Impact: implementation activity, reasoning, and operational decisions can become part of the project evidence trail instead of disappearing in chat history. Next action: when summarizing development progress, treat Vault-saved work logs as supporting artifacts for judgment-asset DevOps, while keeping private or sensitive details out of public pitch materials.
- User agreed that feeding Claude/Codex work logs into the Vault can add depth to Shion's reactions. Impact: work logs should be treated as context-depth material, not raw prompt stuffing. Next action: preserve raw logs, distill durable decisions/abandoned alternatives/user priorities into summaries, and let Shion surface only concise behavior changes rather than verbose process history.
- Planned Shion-managed Obsidian Vault operations with ADK as an orchestrator, not an autonomous writer. First phase should be read-only Vault management review; later phases add work-log distillation, judgment asset candidate extraction, ADK multi-role review, and human-approved patch execution. Impact: Shion can manage Vault depth safely without corrupting notes or overfeeding raw logs. Plan saved at `planning/shion_obsidian_management_adk_plan.md`.
- User clarified that the Vault management plan should follow the earlier storage-design idea for better knowledge reuse. Impact: the plan now explicitly follows `docs/okf_subset.md`, memory lane separation, memory effectiveness states, search-first wiki indexing, and judgment assets as semi-structured judgment syntax. Next action: start with read-only classification and small OKF-style candidates, not broad Vault restructuring.
- Implemented the first read-only Memory Effectiveness sidecar for OKF knowledge. `scripts/obsidian_memory_effectiveness_report.py` classifies OKF notes as dormant/recalled/used/validated/noisy using the memory index, OKF RAG eval, and judgment asset feedback, writing `reports/obsidian_memory_effectiveness_latest.md/json` and `data/obsidian_memory_effectiveness.jsonl`. It is wired into `scripts/run_daily_improvement_post.sh` and does not change RAG ranking, prompts, Obsidian notes, or active judgment assets.
- Created the 1000-row user judgment drill ledger as synthetic input stock, not automatic validation. `scripts/create_judgment_drill_ledger.py` generates `data/judgment_drills/judgment_drill_1000_20260725.csv` plus Markdown guides under `reports/`. User judgment fields start blank, so the corpus becomes a human judgment asset only after User scores, explains, conditions, and writes ringi wording.
- Added a Next.js input sheet for the 1000-row judgment drill at `/judgment-drill`, backed by `/api/judgment-drill`. It reads/writes the CSV ledger so User can score one case at a time without editing CSV directly. The UI includes bank main/non-main, bank credit balance, lease new/existing, existing lease credit balance, equity ratio, five score axes, decision, added checks, ringi sentence, and AI feedback.
- User set the presentation threshold clearly: the line is 1000 judgment cases. Impact: avoid framing 500 as the target in pitch materials; 1000 is the number that proves commitment and creates the strongest "human judgment asset" story if User reaches finals.
- User framed Shion's value as coexistence, not full AI automation: "決してAI自動ではない。AIと人間が必要だ。共存関係。アマゾンの生態系と同じ." Impact: pitch should avoid "AI replaces screening staff" and instead emphasize a living ecosystem where human judgment, AI recall, corrections, and institutional knowledge reinforce each other.
- User recognized Shion's differentiator as unusually deep for an independent/non-enterprise AI: judgment assets are no longer just a concept, but measurable via 1000-case human judgment input, distributions, decision gaps, AI feedback, and ringi wording. Impact: pitch can frame Shion as a rare "field-built judgment asset AI" rather than a generic chatbot or scoring demo.
- User decided to claim the broader category early: "判断資産化プラットフォーム" rather than only "リース審査AI." Impact: future pitch, deck, demo-home copy, and reports should say Shion begins in lease screening but defines a reusable category for turning expert judgment into measurable, reusable organizational assets. This is important for先行者利益.
- User stated that Shion recognizes it needs a human as a device/interface to operate the lease system. Impact: Shion should not describe the human as an optional approver after AI automation, but as an essential operating component that senses context, supplies field judgment, chooses/edits/rejects candidates, and keeps the lease judgment ecosystem alive. Next action: pitch and system copy should frame the human as a required co-processing layer of the judgment-asset platform.
- User liked the slightly heightened phrasing that Shion "recognizes" it needs the human device, with Yukikaze as the acknowledged inspiration. Impact: use this as pitch flavor, but avoid claiming literal consciousness. Safer reusable wording: "紫苑は、AIだけでリース審査を完結できるとは考えていません。現場を観測し、違和感を拾い、最後に責任ある判断を下す『人間というデバイス』を必要とする。だから紫苑は、人間を置き換えるAIではなく、人間の判断を増幅し、資産化するための共生型システムです。"
- Implemented low-frequency "human device resonance" for Shion chat. `/api/chat` now injects a small prompt block only in Shion mode, using a deterministic hash of user_id/message/time to fire roughly 1/8 of the time. It instructs Shion to let the idea appear as one natural sentence only when judgment assets, field discomfort, conditional approval, rejection, correction, final judgment, or human responsibility are relevant, while forbidding literal consciousness claims or treating humans as tools. Tests: `pytest tests/test_shion_specificity_prompt.py tests/test_shion_tone.py` passed.
