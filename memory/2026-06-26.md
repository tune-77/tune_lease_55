# 2026-06-26

## Work Log

- README.md を親しみやすい構成へ整理した。起動方法、紫苑、Private Reflection、Obsidian連携、開発方針を短く読める形にした。
- Private Reflection が3日連続で同じ文面になっていた原因を調査した。04:01時点で対話ログ・日次メモが薄い場合、固定フォールバック文に落ち、その後の対話増加で再生成されていなかった。
- `lease_intelligence_reflection.py` のフォールバックを修正し、日付別の導入文、日次レポート材料、古い固定文の引用除外を追加した。
- 2026-06-24、2026-06-25、2026-06-26 の通常Vault Private Reflection を再生成した。24/25はfallback、26はGemini本文。
- `lease_intelligence_central.py` は、mind.jsonのキーポイントをGeminiへ送る可能性があるため、デフォルトではローカル観察へ退避する設計に変えた。外部送信は `ENABLE_CENTRAL_GEMINI=1` の明示時だけ。
- Private Reflection を「書いて終わり」ではなく、差分を見る、重要点を記憶へ昇格する、次回対話プロンプトへ戻す閉ループにした。今日分の通常Vault Private Reflectionにも `## 差分と再利用` を追加し、`mind.json` の `private_reflection.next_context` へ戻した。
- 毎日収集しているリースニュースが保存で止まりがちだったため、日次ニュースを審査アクションへ変換する閉ループを追加した。`lease_news_digest.py` に `LeaseNewsAction` / `write_lease_news_actions_note()` / `lease_news_actions_as_text()` を追加し、収集後に `Projects/tune_lease_55/News/YYYY-MM-DD_lease-news-actions.md` と `data/lease_news_actions_latest.json` を生成する。チャットと軍師AIは「この案件に効くニュース影響」として参照する。

## Promotable Items

- Private Reflection は保存成功だけでなく、日別差分・材料鮮度・後追い再生成の有無を見る必要がある。
- Vault内の私的記憶を外部LLMへ送る処理は、明示許可か環境変数ゲートを要求する。
- 今日分の日次メモがないと、朝の内省フォールバックが薄くなりやすい。
- 内省は、`書く -> 差分を見る -> 昇格候補にする -> 次の対話や判断で使う` まで接続して初めてAI機能として意味が出る。
- ニュースも、`収集 -> ノイズ判定 -> 審査アクション化 -> 案件文脈へ注入 -> 効果測定` まで接続して初めて判断資産になる。
