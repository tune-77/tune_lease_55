# 2026-05-26

## Snapshot
- Context: legacy raw daily note.
- Action: captured the day's operational details below.
- Result: keep the full log intact for later review.
- Next: promote only repeated or decision-shaping items into `MEMORY.md`.

- Configured `com.tunelease.obsidian-backup` as a weekly LaunchAgent. It now runs every Sunday at 04:00, keeps 14 backups, and was loaded into `~/Library/LaunchAgents`.
- Initial `RunAtLoad` backup succeeded: 161 files, 5.5 MB, destination under iCloud Drive `tune_lease_55_backups/obsidian/Obsidian Vault_20260526_041827`.
- Added `com.tunelease.weekly-health-check` as a weekly LaunchAgent for Sunday 05:00. It checks Obsidian reindex freshness, backup freshness, lease news freshness, Chroma DB freshness, and SQLite integrity for `lease_data.db` / `screening_db.sqlite`. Initial launchd run succeeded with all checks OK.
- Added weekly case-data backup automation. `com.tunelease.case-data-backup` runs every Sunday at 04:30 and writes timestamped snapshots under iCloud Drive `tune_lease_55_backups/case_data`, keeping 12 generations. The backup uses SQLite's backup API for `.db` / `.sqlite` files and copies core learning/config JSON files. Initial manual run succeeded with 12 files / 14.8 MB, and the weekly health check now verifies case-data backup freshness.
- Prepared Obsidian desktop/iPhone sync via iCloud Drive. Copied the existing vault from `~/Documents/Obsidian Vault` to `~/Library/Mobile Documents/com~apple~CloudDocs/Obsidian/Obsidian Vault` without deleting the original, updated LaunchAgents/scripts and `OBSIDIAN_VAULT` to use the iCloud vault, refreshed the Obsidian app vault path, collected the 2026-05-26 news note into the iCloud vault, and rebuilt the Chroma index from the iCloud vault.
- iPhone Obsidian did not show the `com~apple~CloudDocs/Obsidian` copy, so moved the active sync target to Obsidian's iCloud app container: `~/Library/Mobile Documents/iCloud~md~obsidian/Documents/Obsidian Vault`. Updated launchd jobs, `.zshrc`, Mac Obsidian vault path, copied the vault there, ran news collection, and rebuilt Chroma from that app-container vault.
- Added voice input buttons to the AI chat surfaces: Next `/chat` uses a lucide mic button with Web Speech API support, and the mobile HTML AI chat card has a microphone button that appends Japanese speech recognition results into the chat textarea.
- Restarted the Next/FastAPI stack with `PUBLIC_TUNNEL=1` so Cloudflare Tunnel starts alongside `run_next_stable.sh`. Added project skill `.agents/skills/restart-next-cloudflare` to standardize future "restart with Cloudflare" requests.
- Added Next `/knowledge-space`, a Three.js 3D Obsidian knowledge graph that visualizes indexed Chroma note nodes, category clusters, wikilinks, and external linked notes. Added `/api/knowledge/graph` and sidebar navigation "知識宇宙マップ"; verified build, API graph output, Cloudflare route, and WebGL canvas nonblank rendering via Playwright Core.
- Refined `/knowledge-space` to look more like a galaxy: spiral-arm placement, denser star-dust particles, smaller node spheres, subtler link lines, central glow, and a more top-down camera angle. Rebuilt/restarted with Cloudflare and verified WebGL screenshot output.
- Updated `/knowledge-space` nodes from sphere-like planets to additive glowing star sprites. Link count now drives star brightness, size, and outer flare, and connection lines render as faint blue-white light instead of dark web lines.
- Added moving route lights to `/knowledge-space`: the last five returned graph edges are highlighted with faint additive lines and animated leading/trailing light sprites that travel along each connection path.
- Expanded `/knowledge-space` with search constellation highlighting, timeline slider, top linked "恒星" list, selected-node ripple routes, and AI evidence-route handoff from Next `/chat`. Graph API now returns node/edge mtime and edge recent_rank so recent routes and temporal filtering can use indexed note freshness.
- Improved `/knowledge-space` mobile layout: desktop control/detail panels collapse behind icon buttons on phones, the page header is compact, and the Three.js camera/root offset is adjusted so the galaxy remains visible instead of being covered by controls.
- Cleaned Mebuki floating assistant display so Markdown markers such as `**bold**`, single asterisks, headings, and inline backticks are stripped before rendering in the bubble/chat panel.
- Adjusted Mebuki floating assistant so long replies do not cut off: bubble visibility now scales with message length, long bubbles scroll, the chat panel uses responsive viewport height, and message text wraps instead of clipping.

## Promotable Items
- Preserve exact decisions, failure modes, and verification details when they matter.
- If a rule repeats across days, it should move to `MEMORY.md`.
