# 2026-05-24

## Snapshot
- Context: legacy raw daily note.
- Action: captured the day's operational details below.
- Result: keep the full log intact for later review.
- Next: promote only repeated or decision-shaping items into `MEMORY.md`.

- Fixed AI screening advice around operating margin comparison. Negative operating margin is now treated as red-flag profitability even when the industry benchmark is also negative, so advice no longer says it is simply above average.
- Extended the same negative-value guard to other indicators that can go below zero: gross margin, ordinary/net margin, equity ratio, ROA, and ROE. Indicator tables and AI prompts now avoid treating negative values as strengths just because an industry benchmark is more negative.
- Improved Next Gunshi Bayes display: `/api/gunshi/stream` now returns structured Bayes factors, A/B/C resale grades are normalized to 高/中/低 before posterior calculation, and `GunshiAdvice.tsx` shows factor cards under the Bayes gauge instead of only percentages.
- Fixed the Next 5-year sales forecast simulation. `RealGraphs.tsx` now sends the currently entered revenue to `/api/timesfm/financial_paths` in thousand-yen units, the API appends that current revenue when past company history is missing/stale, and the chart displays values consistently in million yen.
- Reduced wasted Next/FastAPI restart time. `run_next_stable.sh` now reuses already-running services by default, skips `next build` when frontend sources are unchanged, sets a stable PATH for non-login shells, and only kills/replaces processes when `FORCE_RESTART=1` is explicitly set.
- Added a Next qualitative-form field for field visit notes backed by existing `passion_text`, with Japanese browser speech input via Web Speech API and graceful unsupported/error states. Also removed the unsupported Next 16 `eslint` config key so `npm run build` type-checks successfully.
- Added a YUKIKAZE tactical mode plan implementation to the Next Gunshi panel: localStorage-backed mode switch, FFR-41MR console styling, score-linked JAM status lines with warning/alert/critical blinking, and `humor_style="yukikaze"` prompt behavior in `build_gunshi_prompt()`.
- Refined YUKIKAZE mode so CRITICAL and the send area show the original app line `I identify the enemy. You decide whether to engage.`, and `/api/gunshi/chat` now forwards `humor_style` into `build_gunshi_prompt()` so clipped Rei-like user prompts get a cold YUKIKAZE-style answer.
- Added the short difficult-case YUKIKAZE callsign `GOOD LUCK, FUKAI LT.` to WARNING/ALERT/CRITICAL UI status lines, the send-area hint, and YUKIKAZE prompt guidance.
- Split MISSION output tone by humor mode so `standard`, `yanami`, and `yukikaze` all keep the same substance but differ in style; standard is terse executive, yanami is snarky, yukikaze is tactical/log-like.

## Promotable Items
- Preserve exact decisions, failure modes, and verification details when they matter.
- If a rule repeats across days, it should move to `MEMORY.md`.
