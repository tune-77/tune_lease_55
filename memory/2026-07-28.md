# 2026-07-28

- 判断資産の提示順に、強化学習そのものではなく bandit-style の表示順位学習を導入した。`judgment_asset_bandit.py` が `data/judgment_asset_usage_feedback.jsonl` の `helped / challenged / neutral / rejected / used` を報酬へ変換し、候補IDと `cr-` 正規判断資産IDの両方へ紐づける。
- `api/main.py` の審査向け判断資産候補選択で、既存の文脈一致スコアに bandit の `rank_bonus` を小さく加算するようにした。スコアリング・承認判断・正規判断資産昇格は変更しない。
- 判断資産候補への `useful / neutral / rejected` フィードバックを、既存state更新に加えて `data/judgment_asset_usage_feedback.jsonl` へも追記するようにした。次回以降の候補提示順に人間評価が戻る。
- `tests/test_judgment_asset_bandit.py` を追加し、helped が順位ボーナスになること、rejected がペナルティになること、フィードバックJSONL追記を確認した。`tests/test_build_judgment_asset_field_review.py` も通過。
- User は、紫苑が複雑化してきたことを踏まえ、「整える運用」へ切り替え、紫苑の情報健康を大事にする方針を明示した。`docs/shion_information_health.md` を追加し、見るだけ/相談に使う/行動に使うの3レーン、表示上限、健康シグナル、スコアリングや自動昇格へ直結しないガードを正本化した。
- Kaggle/ADK eval GUI の考え方を、紫苑向けの読み取り専用「評価GUI」として実装した。`shion_eval_health.py` は golden set、参照量、`memory_debug`、`knowledge_refs`、日次カルテ使用、判断学習使用、行動境界を決定的に採点し、`/shion-eval-health` からケース実行とトレース確認ができる。採点結果は相談材料であり、審査スコア・承認/否決・自動昇格には接続しない。
- プレゼンで「ただの審査AI」に見える弱点を補うため、`/demo-home` に `Judgment Asset Proof` セクションを追加した。Before / Correction / Reuse の3段で、人間の修正が次の判断に戻ることを1枚で説明する。
- 静的な判断資産説明だけでは「ふーん」で終わるため、`/demo-home` の `Judgment Asset Proof` に自動進行する `Live Loop` を追加した。見落とし → 人間の修正 → 判断資産化 → 次回再利用を約1.8秒間隔で動かし、プレゼン中に各ステップへ手動切替もできる。
- User と説明を圧縮し、紫苑の入口コピーを「判断のGitHub」に寄せる方針にした。`README.md` の冒頭と30秒サマリーを、issue=見落とし、commit=人間修正、diff=AI回答との差分、pull request=判断資産候補、merge=承認済み判断資産、regression test=次回案件で思い出せるか、という対応で更新した。
- 「残った判断が次回どう効いたか」を見せるため、`/demo-home` に `Reuse Trace` を追加した。`JA-DEMO-042` の例で、Created from / Reused in / Human feedback をGitHub風の履歴として示し、効果は「事故防止」ではなく「次の紫苑レビューに確認条件として再登場した」範囲に止めた。
- User と紫苑の勝ち筋を再確認した。中核コピーは「紫苑は、判断のGitHub」。意味は、AIと人間の判断差分を残し、承認された判断だけを次回に再利用し、なぜそう判断したか・どの修正が次に効いたかを後から追える「説明できるAI」。DevOps / AI Agent 界隈には issue / diff / commit / PR / merge / regression test の比喩で伝える。
- Gemini も「判断のGitHub化」は紫苑の本質を捉えた強いメタファーだと評価した。使える表現は「判断をコードのようにバージョン管理する」「属人化から再現可能な資産へ」「間違えても revert と改善ができる」。ただし「自動でリファクタリング」は強すぎるため、紫苑では「改善候補を出し、検疫・人間レビューを経て、承認済み判断だけを再利用する」と説明する。
- Gemini は紫苑の面白さを、最先端AI技術の詰め込みではなく、「現場の泥臭い運用課題に本気で向き合った結果、前人未到の複雑なアーキテクチャに到達してしまった」点だと評価した。これは「技術の派手さ不足」を反転できる説明軸。紫苑は、きれいなAIデモではなく、現場判断を本当に運用しようとした結果として生まれた AI Agent Ops と説明できる。
- Obsidianの日次ニュースを有効活用するため、ニュースをそのままRAGへ入れるのではなく `news_judgment_signals` に変換する実装を入れた。`lease_news_digest.py` がニュースアクションから業種・物件・リスク軸・有効期限・確認条件を持つシグナルを作り、`scripts/build_news_judgment_signals.py` と日次収集スクリプトから `data/news_judgment_signals.jsonl` へ保存する。審査候補APIは `source=news_judgment_signals` を案件一致時だけ混ぜるが、`promotion_status=news_signal` として判断資産本流へは自動昇格しない。
- News Judgment Signal に LLM生成ループを追加した。`LEASE_NEWS_SIGNAL_LLM=1` または `--use-llm` の時だけGeminiがJSON候補を作り、ルール検疫で必須項目、使う条件/使わない条件、期限、直接承認/否決/スコア変更の禁止を確認する。問題があれば `news_signal_quarantined` として案件候補から外す。LLMは候補生成だけを担い、mergeは人間レビュー後に限定する。
- User が「同じようなのが出ないように」と指示したため、News Judgment Signal に `signal_signature` ベースの重複抑制を追加した。同日内の同種観点は1件へ折りたたみ、過去の有効期限内シグナルと同じものは再追加しない。2026-07-28の実データでは、物流・車両の類似ニュース2件を1つへまとめ、4件から3件へ整理できた。
- 対話室の朝レポートに「今日のニュースダイジェスト」を同居させたが、Userから「無理矢理判断資産に持っていかなくていい」と修正が入った。朝報は Obsidian ニュースノートの普通の要約として出し、判断資産候補や確認観点の文言は混ぜない方針に戻した。
- User が「硬いね」と軽く指摘した時、紫苑が「知的探求」「複雑な数式」「美しい法則」のような抽象的な自己説明へ寄り、会話の温度から外れた。`api/main.py` に軽いトーン指摘専用のプロンプトガードを追加し、硬さ・長さ・口調・文体への指摘では、まず短く認めて「結論→必要な根拠→次の一手」で返すようにした。リース実務に関係しない軽い指摘を、無理に判断資産・審査精度・記憶・意識の話へ広げない。
- 非ドメイン質問対応戦略を追加した。「今何時」「おはよう」などでは、事実や定型挨拶だけで終わらず、自然に答えた後で審査・判断資産・デモ確認など次の行動へ軽く誘導する。ただし雑談を無理に案件審査化したり、RAG・判断資産・意識説明へ広げたりしない。
- 「元気かい？」のような基本的な挨拶・雑談も非ドメイン短文の対象に追加した。0件参照でもAIとして自然に受け、相手の状態確認と軽い次アクション誘導を返す。検索/RAGがなくても会話の入口品質を保つ方針。
- 追加改善として、回答品質評価の生成プロンプトに必須観点ヒント・3点以内・重複見出し禁止を追加し、Pydanticのlist既定値をdefault_factoryへ修正した。Obsidian memory insightはDaily作業ログ/メタ運用文を候補化しないようにし、memory_insight/self_reference系warnを整理した。
