# 2026-05-23

## Snapshot
- Context: legacy raw daily note.
- Action: captured the day's operational details below.
- Result: keep the full log intact for later review.
- Next: promote only repeated or decision-shaping items into `MEMORY.md`.

- Added the lease advisor MVP inside the existing AI consultation UI as a new `🤖 リース参謀` tab instead of adding a separate page/API/DB.
- Created `lease_advisor_logic.py` with `build_lease_advisor_prompt()` and fixed output headings for sales-oriented lease advice.
- Wired the advisor tab to `last_result`, `last_submitted_inputs`, Obsidian context via `obsidian_ai_context.py`, existing subsidy matching, and past competitor stats.
- Verified syntax with `python -m py_compile lease_advisor_logic.py components/ai_consultation.py` and prompt constraints for general/subsidy/competitor focus.
- Improved the NEXT Gunshi AI without breaking the existing Yanami/chat area: `/api/gunshi/stream` now emits deterministic `strategy_cards`, and `GunshiAdvice.tsx` renders an "案件作戦盤" above the existing chat.
- Fixed a blocking `build_gunshi_prompt()` syntax issue in `shinsa_gunshi_logic.py` that prevented FastAPI from importing the Gunshi modules.

## Promotable Items
- Preserve exact decisions, failure modes, and verification details when they matter.
- If a rule repeats across days, it should move to `MEMORY.md`.
