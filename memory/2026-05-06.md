# 2026-05-06

- `past_cases` の成約/失注 1526件で AUC を確認。
- 現行 `score` の AUC は 0.6268、`score_borrower` は 0.6350。
- 定量 39特徴の OOF AUC は LR 0.6769、MLP 0.6865 で、現行スコアより良い。
- DB保存済みのスタック用列は `bench_score` / `ind_score` / `yield_pred` / `contract_prob` などが多く欠損しており、単純な stacking AUC は過信しない方がよい。
- QCL の再計算では、同じ 2-fold 条件の OOF AUC が `LR 0.6670`、`LGBM 0.7493`、`QCL 0.5175`。`LR+LGBM` のブレンドは `0.7510`、`+qcl_prob` を足しても `0.7508` で改善はほぼなし。
- QCL は本番導線から外した。`train_quantum.py` の QCL CLI/ helper を削除し、`quantum_qcl.py` と生成レポートも除去した。
- `analysis_regression.py` に `customer_new` / `deal_source_bank` / `dscr_approx` / `interest_coverage` を追加し、`lgb_main_model.joblib` を再学習。`run_quantitative_contract_analysis()` の train/test では `auc_lgb 0.7834`、`ensemble_alpha 0.0`。5-fold OOF では `LR 0.7085`、`LGBM 0.7804` まで上がった。
- セグメント別 OOF AUC も確認。`全体_既存先` は `LR 0.6909 / LGBM 0.7721`、`全体_新規先` は `0.6391 / 0.6439`。業種別では `サービス業_既存先` の `LGBM 0.8065` が強く、`医療_新規先` は `0.4192` と弱かった。小件数セグメントは不安定。
- `score` の本体 LGBM を新規/既存で分割し、アンサンブルは廃止。`lgb_main_model.joblib` を既存先、`lgb_main_model_new.joblib` を新規先として保存し、`score_borrower` は customer_type で切り替えるように変更した。
- 定性側も整理し、`score` への定性LGBM混入と `ensemble_config_qual.json` を削除。定性画面は LR と LGBM の個別比較だけ残した。
- `bench_score` / `ind_score` は DB の `past_cases` へ全件バックフィルした。`labeled=1507`、`score_borrower=1507`、`bench=1507`、`ind=1507`、`all_three=1507` まで揃い、3本ブレンド重みの再最適化結果は `w_main 0.3149 / w_bench 0.0 / w_ind 0.6851`、`auc_cv 0.7029` だった。今後は欠損前提の重み推定ではなく、全件での再評価を前提にできる。
- バッチ審査は `bench_score` / `ind_score` を手入力ではなく自動計算へ切り替えた。CSV テンプレートと OCR 変換列からは外し、保存 JSON では `bench_score` / `ind_score` / `ind_name` を毎回埋めるようにした。
- `score_borrower` 周辺の表現を単体モデル前提に整理した。`analysis_results.py` と `score_dag.py` のブレンド文言を外し、`settings.py` の再学習ボタンも LightGBM 単体の再学習表記に寄せた。README も LR+LGBM アンサンブル前提の説明を削除した。
- 審査結果画面に参考AUCと差分アラートを追加した。過去案件の `score` から参考AUCを出し、`score_borrower` / `bench_score` / `ind_score` の乖離が大きいときは「参考比較・差分アラート」を表示する。DAG でも差分警告ノードを出すようにした。
- 非LR比較を実施。`LGBM / RandomForest / ExtraTrees / MLP / XGBoost` を OOF AUC で比較したところ、単体は `RandomForest 0.8036`、`XGBoost 0.7940`、`ExtraTrees 0.7800`、`LGBM 0.7596`、`MLP 0.7239`。上位3モデルの stacking も試したが、`RandomForest` 単体を超えなかったため、現時点では stacking 採用なし。
- `score_borrower` の本体モデルを RandomForest に切り替えた。`data/lgb_main_model.joblib` と `data/lgb_main_model_new.joblib` を RF で再学習し、`scoring_core.py` は既存/新規の RF バンドルを読むようにした。README と画面文言も RF 前提に更新済み。
