# 2026-06-02

## Codex 自動改善キュー

- 改善リストから Codex が安全に触れそうな候補を毎朝最大3件に絞る `scripts/build_codex_auto_queue.py` を追加。
- 4:00 の `scripts/run_daily_improvement_pipeline.sh` でキューを生成し、`reports/latest.json` へ件数メタデータを同期する方針にした。
- 6:00 の `scripts/aurion_core_daily.py --mode morning-report` は `Codex Auto Improvement Queue` として安全候補数、今日のキュー、保留候補、手動/ブロック件数を朝レポートと通知に出す。
- 現時点では「自動でコード修正まで実行」ではなく、朝の報告に実行候補を出す段階。実行まで自動化する場合は Codex の安定した実行エントリポイントと失敗時停止条件を別途固定する。

## 改善申告漏れチェック

- 手動またはCodexで直したのに `REV-xxx` を済登録し忘れるケースに備え、朝レポートへ `Improvement Declaration Gap Check` を追加。
- 直近36時間のgitコミットと最新改善レポートを突合し、明示REV-IDが未登録のまま残っているもの、またはREVなしコミットとタイトルが近いものを「申告漏れ疑い」として表示する。
- 自動で `applied` にはしない。誤って済にするより、疑いとして朝報告へ出して人間確認に倒す方針。

## Codex作業ログのObsidian保存

- Codex/Claudeとの会話全文ではなく、作業要約・意思決定・変更ファイル・検証・commit/merge SHAだけをObsidianの日次ノートへ残す方針にした。
- `.agents/skills/obsidian/scripts/obsidian_note.py codex-work-log` / `claude-work-log` / `agent-work-log` を追加し、`git-ship` 完了後にこの形式で記録する手順を追加。
- 機密情報、DB生データ、会話の試行錯誤は保存せず、将来の改善申告漏れチェックと監査に必要な粒度だけを残す。

## Codex利用制限時の保留扱い

- Codexが課金条件・利用制限で止まった改善IDは `applied` にせず、`blocked_by_quota` として `reports/codex_auto_execution_status.json` に記録する方針にした。
- `scripts/record_codex_auto_status.py` を追加し、`REV-xxx --status blocked_by_quota` で明示的に保留登録できるようにした。
- 次回のCodex自動改善キューでは `blocked_by_quota` のREVを除外し、朝レポートの `Blocked by Codex Quota` に表示する。

## Obsidian 第二の脳運用の第1段階

- X記事の `raw / wiki / reports + Skills` 方式を参考に、既存Vault構造は壊さず「共通検索経路・根拠付き保存・wiki昇格キュー」を追加する方針にした。
- `mobile_app/semantic_search_engine.py` の直接Vault走査を `mobile_app/obsidian_bridge.py` の `iter_indexed_obsidian_documents()` 経由へ変更し、AIチャット系のObsidian検索ルールに寄せた。
- `kb-report` Skillを追加し、wiki/knowledge notesを根拠に `reports` へ `used_wiki_pages` / `source_notes` 付きで残す運用を定義した。
- Wiki自動保存と物件ファイナンス保存に `Source Metadata` を追加し、生成クエリ・参照ノート・使用wikiページを追えるようにした。
- `scripts/build_wiki_promotion_queue.py` を追加し、チャットログ・Daily・改善ログから正式wiki化候補を毎朝最大3件抽出する。2026-06-02時点では候補55件、キュー3件。
- `scripts/promote_wiki_queue.py` を追加し、キュー候補を `Projects/tune_lease_55/Asset Knowledge/Promoted Knowledge.md` などの正式ノートへ自動昇格する処理を実装した。
- 2026-06-02のキュー3件は実Vaultへ昇格済み。`reports/wiki_promotion_status.json` で `promotion_id` ごとに重複防止し、再実行時は `skipped_duplicate` になることを確認した。
