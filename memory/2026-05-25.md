# 2026-05-25

- Fixed `run_next_stable.sh` restart cleanup after duplicated Next/FastAPI/Cloudflare Tunnel processes caused port conflicts. The launcher now tracks supervisor PIDs, stops stale launchers more reliably, kills stale Cloudflare quick tunnels by target URL, and records the real `cloudflared` PID instead of a pipeline wrapper.
- Unified Obsidian chat retrieval around `api.knowledge.vector_store.KnowledgeVectorStore`: `mobile_app.obsidian_bridge.search_notes()` now uses Chroma-backed RAG first and falls back to the existing Vault search, while `/api/chat` uses the same store instead of a separate Chroma query path. The vector store no longer tries HuggingFace by default; offline mode uses indexed Chroma documents plus deterministic hash embeddings for reindex/upsert stability.
- Downloaded the multilingual MiniLM SentenceTransformer model into `models/sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2`, taught Obsidian RAG to auto-use that local path, ignored the large local model directory in git, and rebuilt the `obsidian_knowledge` Chroma collection with 846 vectorized chunks for stable semantic search.
- Added an Obsidian RAG evaluation set and evaluator script, then tuned retrieval reranking to prioritize lease knowledge/project notes, demote daily/chat/clipping noise, and suppress humor/character notes unless the query asks for them. Current eval: Hit@1 10/10, Hit@5 10/10, MRR 1.000, forbidden noise 0/10.
- Shortened Mebuki AI chat behavior: normal answers are now constrained to roughly 300 Japanese characters, conclusion plus up to 3 bullets, with detailed explanations only when the user asks for them. Gemini chat output is also capped by `MEBUKI_CHAT_MAX_TOKENS` with a default of 420.
- User stated the product ambition clearly: build the world's best lease system. Treat this as the north star for future prioritization: every feature should improve real lease screening judgment, operational reliability, explainability, knowledge reuse, or field usability.
- User clarified the deeper purpose: "I want to systematize and preserve all the know-how I have learned. That's all. I don't know how far I can go." Record this as the core motivation behind the lease system: convert Kobayashi's accumulated lease screening judgment, field intuition, failures, successes, and review standards into a durable, searchable, reusable system.
