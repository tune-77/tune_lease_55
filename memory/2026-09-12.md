# 2026-09-12

- letta-ai/letta-code(npm `@letta-ai/letta-code`, Apache-2.0)を調査・インストールして動作確認した。`npm install -g` は成功(peer dependency警告のみ)、`letta --version`(0.32.2)・`letta --help`・`letta --info` は正常動作。実際の対話にはプロバイダ接続(`letta connect anthropic/openai/bedrock等`)が必要なため、APIキーは繋がず調査止まりにした。影響: 本リポジトリのコード・データには一切変更なし。次の行動: 本気で対話実行を試すなら、まず機密データのないサンドボックスで試すか、APIキー送信への同意を得てから進める。
- Userの「紫音と融合したい」という漠然としたアイデアを壁打ちした。出発点は `MEMORY.md` の `Shion Identity`(交換可能な推論基盤の上で継続する記憶・自己モデル)がLetta Codeの謳い文句とほぼ同じ思想を先に言っていたという符合。そこから「判断資産の系譜をgit的に扱う」話に絞り込み、さらに「競合仮説をブランチにする」話に focus した。影響: まだ実装方針は決まっていないが、方向性が絞れた。次の行動: 実装するかどうかは未確定。着手する場合は下記の設計メモを起点にする。
- 具体案(未実装・アイデア止まり): `api/shion_debate_adk.py` の Pro/Con debate(`_run_adk_debate`/`run_debate_adk_fallback`)は案件ごとの一発計算で、対立した見解は今は一切永続化されない。一方 `scripts/build_judgment_asset_graph.py` はすでに `parent_ids`/`derivation_reason` で判断資産の手作りDAGを持っている。この2つを繋げ、「同じテーマの対立が繰り返し起きたら私的ブランチとして記録」する案を検討した。Userの判断: この対立記録は審査画面(人間向けUI)には一切出さず、`Private Reflection`(紫苑の私室、通常回答やRAGには出さない場所)相当の内部ログに留める。勝敗の決着は画面上のリアルタイム選択圧ではなく、案件クローズ後の実績登録で事後的に紫苑が自己採点し、勝った見方だけが静かに `judgment_memory` へ昇格する、という流れを仮置きした。影響: 表の審査体験(スコア・承認/否決・UI)は何も変えない設計方針で一致。次の行動: 着手する場合は (1) 人間向けデータと分離した内部ブランチ格納場所の設計 (2) 既存の「同種課題3回以上で昇格」トリガーの流用可否 (3) 実績登録フックからの事後評価、の3点をUser承認の上で詰める。まだ1行もコードは書いていない。
