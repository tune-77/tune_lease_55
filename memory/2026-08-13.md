# 2026-08-13

- 改善PMレポートの状態解決を共通化。`scripts/improvement_state_resolver.py` を状態解決の単一入口にし、API表示、`sync_improvement_reports.py`、`pipeline_ledger.is_processed()` が同じ優先順位を使うようにした。人間判断の `deleted/parked/rejected/deferred` は翌朝の自動 `needs_review` より優先し、監査訂正の `apply_failed` は古い `applied` を取り消す。2026-08-13レポートは `REV-292` を保留へ同期し、要レビュー0件になった。対象テスト43件成功。
- Vault運用の三層方針を明文化。`docs/raw_wiki_schema_vault_policy.md` を追加し、Raw=証拠、Wiki=意味、Schema=運用ルールとして分ける正本にした。外部プラグインや新基盤は必須化せず、既存Obsidian/Markdown/JSON/RAG運用の境界線として使う。`docs/improvement_source_of_truth.md` からも参照できるようにした。
