# 2026-07-13

## Work Log

- Backfilled Q_risk discovery fields into `data/demo.db` for all reusable demo data.
- Updated all `past_cases` rows with `q_risk_score`, `quantum_risk`, `q_risk_level`, `q_risk_role=discovery_signal`, `q_risk_patterns`, `q_risk_reasons`, and `qrisk_similarity_text`.
- Updated all `screening_records` rows with `q_risk_score` and Q_risk metadata inside `input_snapshot`.
- Mirrored the updated demo DB to `.cloudrun_bundle/data/demo.db` and `.cloudrun_bundle/data/lease_data.db`.
- Created backups under `data/backups/*before_qrisk_backfill_20260713.db`.
- Verification: SQLite `PRAGMA integrity_check` returned `ok`; Q_risk missing count is 0 for both `past_cases` and `screening_records`; `scripts/check_cloudrun_demo_readiness.py` passed.
- Deployed the updated API bundle to Cloud Run service `tune-lease-55-api`; revision `tune-lease-55-api-00033-px5` is serving 100% traffic.
- Confirmed the existing service status URL remained `https://tune-lease-55-api-6mijhyebkq-an.a.run.app`; both cloud-status and `/api/score/full` returned 200 on that URL.
- Added a Cloud Run demo reaction-to-judgment-asset loop. Explicit user reactions (`human_response_feedback`, `screening_loop_feedback`, `shion_screening_review_feedback`, `judgment_feedback_created`, `lease_news_judgment_change`) now sync into quarantine table `cloudrun_judgment_asset_candidates`.
- Updated `/api/cloudrun-return-review` and the Cloud Run return review UI to include `judgment_asset` as a reviewable kind. Approved candidates promote to `data/demo.db` table `judgment_asset_candidates`, not to the scoring model.
- Deployed the reaction loop to Cloud Run. API revision `tune-lease-55-api-00034-hdl` and Web revision `tune-lease-55-web-00012-j59` are serving 100% traffic. Existing status URLs stayed `https://tune-lease-55-api-6mijhyebkq-an.a.run.app` and `https://tune-lease-55-web-6mijhyebkq-an.a.run.app`.
- Added Qrisk panel buttons for human judgment capture: “信用ではなく競合/成約リスク” and “信用リスクとして警戒”. They post `screening_loop_feedback` to the existing writeback path, so Qrisk findings can become judgment-asset candidates without mixing credit risk and sales/competition risk directly into the score.
- Deployed the Qrisk button UI to Cloud Run Web revision `tune-lease-55-web-00013-48v`. Existing Web URL stayed `https://tune-lease-55-web-6mijhyebkq-an.a.run.app`; `/screening` returned 200 and the judgment asset API returned 200 on the existing API URL.
- Decided the full learning design should separate pre-decision human hypotheses from post-result registration. Qrisk/judgment buttons capture “at that time, what risk did the human think this was?” while result registration captures “what actually happened?” Later comparison can validate whether human judgment patterns were accurate.
- Added README section “ストロングポイント：判断資産を育てるAI” to explain Qrisk, human pre-judgment, result registration, and risk-origin separation as the project's core differentiation.
- Clarified and implemented Qrisk as a two-layer signal: current `quantum_risk` remains the score-external exploration signal, while old `aurion.q_risk` is restored as `financial_consistency_risk` / `financial_consistency_score` for financial/input consistency checks. The Qrisk panel now displays the old financial consistency check separately and includes it in judgment-asset feedback context.
- Deployed the two-layer Qrisk update to Cloud Run. API revision `tune-lease-55-api-00035-jtv` and Web revision `tune-lease-55-web-00014-tpg` are serving 100% traffic. Existing URLs stayed `https://tune-lease-55-api-6mijhyebkq-an.a.run.app` and `https://tune-lease-55-web-6mijhyebkq-an.a.run.app`; `/api/score/full` returned `financial_consistency_risk`, and `/screening` returned 200.
- Hot-started Cloud Run by setting min instances to 1. Final revisions: API `tune-lease-55-api-00037-4sf` with `minScale=1`, `maxScale=10`; Web `tune-lease-55-web-00016-69h` with `minScale=1`, `maxScale=20`. Existing URLs stayed unchanged and both `/api/score/full` and `/screening` returned 200.
- Adjusted the screening demo case `デモフードサービス` / company `900303` so its cautious new-food-service profile no longer appears as a near-approval 71-point case. The deployed API now returns `score=62.8`, `score_base=71.7`, `hantei=要審議`, and `quantum_risk=100.0` on the existing API URL. API revision after deploy and hot-start restoration is `tune-lease-55-api-00039-xwj` with `minScale=1`, `maxScale=10`; existing Web `/screening` URL still returns 200.
- Fixed Private Reflection input freshness. `lease_intelligence_reflection.py` now reads local `data/cloudrun_chat_log.jsonl` directly as a fallback when Obsidian conversion lags, converts Cloud Run UTC timestamps to JST dates, and prioritizes concrete case/Qrisk/judgment-asset/UI signals over generic chat. `scripts/run_daily_improvement_post.sh` now syncs Cloud Run inputs from GCS and writes Cloud Run conversation logs to Obsidian before generating Private Reflection. Synced 2026-07-13 Cloud Run inputs from GCS, wrote Obsidian Cloud Run logs (`chats=10`, `dialogues=3`), and regenerated `Private Reflection/2026-07-13.md`; it now includes `デモフードサービス`, `Qrisk`, and the Cloud Run chat materials with quality gate score 100.
- Fixed a Shion dialogue-room failure where a simple question like “トラックの法定耐用年数は何年？” could degrade into “Obsidian note not found.” Added a local useful-life prompt block in `lease_intelligence_dialogue.py` so statutory useful-life questions use the existing local master even when Obsidian RAG has no direct note. Truck answers now include 5-year general/large/wing/flatbody guidance, 4-year small/3.5t-or-less guidance, and vehicle-class confirmation caveat.
- Added regression tests in `tests/test_lease_intelligence_dialogue.py`; `python -m py_compile lease_intelligence_dialogue.py` and `pytest tests/test_lease_intelligence_dialogue.py -q` passed.
- Broadened the fix from truck-only to a shared deterministic basic lease QA layer in `lease_finance_knowledge.py`. It now covers common statutory useful-life questions, finance lease, operating lease, residual value, movable-property insurance, fixed-asset/depreciable-asset tax, lease-vs-loan/self-funding comparison, and lease-period setting. `lease_intelligence_dialogue.py` keeps this basic QA even in casual mode, and `ai_chat.py` injects it into normal AI consultation prompts.
- Added `tests/test_lease_finance_knowledge.py` and expanded dialogue tests. Verification: `python -m py_compile lease_finance_knowledge.py lease_intelligence_dialogue.py ai_chat.py`; `pytest tests/test_lease_finance_knowledge.py tests/test_lease_intelligence_dialogue.py -q` passed with 10 tests.

## Promotable Items

- Q_risk demo data backfill should treat Q_risk as a discovery/search signal, not an automatic score deduction. Store both numeric score and reason/search tags so similar-case retrieval can explain why a case is relevant.
- Demo reactions should be treated as judgment-asset candidates through quarantine and human approval, not as immediate model training data. Explicit reactions become reviewable evidence first, then approved items can be promoted into `demo.db` for reuse.
- Qrisk human feedback should separate at least two axes: credit concern vs. sales/competition concern. A low score with good performance may still be a “競合・成約リスク” case rather than a credit-negative case.
- Judgment asset learning model: `事前判断 = 人間の仮説`, `結果登録 = 実際の結果`, `差分 = 判断基準の精度検証`. This lets the system learn not only outcomes, but whether the human concern was well-calibrated.
- Keep old Qrisk and new Qrisk conceptually separate: `financial_consistency_risk` checks whether financial/input numbers make sense; `quantum_risk` is the broader exploration trigger. Do not merge them into one score.
- Demo-specific score shaping is acceptable when it protects the hackathon story, but keep it scoped to explicit demo identifiers and avoid changing the general scoring model unless the same issue appears in real cases.
- Private Reflection must not depend only on already-written Obsidian dialogue notes. If Cloud Run logs are available in `data/cloudrun_chat_log.jsonl`, read them directly and treat missing/late Obsidian conversion as a pipeline lag, not as “no dialogue”.
- Dialogue-room RAG must not treat “no Obsidian note found” as “cannot answer” for deterministic local masters. For recurring statutory useful-life questions, inject local master facts before the LLM answers and keep a regression test around the obvious queries.
- Basic lease concepts should have a deterministic prompt source separate from RAG. RAG can enrich and override with fresher notes, but it must not be the only path for elementary questions.
