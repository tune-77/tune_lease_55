# 2026-09-01

## Cloud Run 知識宇宙の表示不足

- 事実: Cloud Run の `/api/knowledge/graph?limit=420` は 13チャンク・2ノートしか返していなかった一方、GCS Vault は1,439 Markdown、ローカルChromaは15,755チャンク・2,942ノートだった。Cloud Runでリクエスト外CPUが止まり、起動後バックグラウンド索引が途中までしか進まないことが原因。
- 影響: `api/routers/pipeline_misc.py` で、Chromaのノート数が要求上限に満たない場合だけ、同期済みMarkdownからファイル名・H2見出し・Wikiリンクを60秒キャッシュで補完するようにした。RAG・審査・スコアリングには接続せず、知識宇宙の描画だけを補う。
- 次の行動: Cloud Runへ再デプロイ後、Web経由の同APIで `vault_fallback_used=true`、`notes>=420` を確認する。将来Chroma同期が健全化した場合は自動的に `graph_source=chroma` へ戻る。
- 追記: 未コミット変更の混入を避けるため、クリーンな一時worktreeから知識宇宙修正だけをイメージ化し、既存サービス設定を保持したままAPIイメージだけを更新した。新リビジョンは `tune-lease-55-api-00101-jpj`、従来URL `https://tune-lease-55-api-6mijhyebkq-an.a.run.app` のまま100%配信。
- 追記: `.gcloudignore` の一般Markdown除外によりbundle Vault本文が2件しか入らないことも判明。本文の新規アップロードは行わず、既にbundle同梱されている `obsidian_retrieval_graph.json` を表示フォールバックに使用した。Web経由の最終実測は `notes=2805 / shown_nodes=466 / links=2238 / limit=420`。
