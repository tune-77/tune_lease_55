# 2026-08-08

- REV-048a `sync_improvement_reports_post` 障害を復旧確認。原因は `reports/latest.json` の `applied` が整数カウンタなのに、`.agents/skills/improvement-report-sync/scripts/sync_improvement_reports.py` が配列として反復したこと。`applied_improvements` / `parked_improvements` へフォールバックするリスト読込ヘルパーを追加し、post 再同期 dry-run と対象 pytest は成功。次回朝バッチでは `sync_improvement_reports_post` の実 exit code が 0 で記録されるか確認する。
- `scripts/run_daily_improvement_post.sh` で post 後処理の成功時に `if` 判定後の `$?` を記録してしまい、成功が失敗として見える箇所を修正。`sync_improvement_reports_post` / `recursive_self_improvement_post` は捕捉した実コマンド結果を `log_step` へ渡す。
