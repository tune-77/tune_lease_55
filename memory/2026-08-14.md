# 2026-08-14

- World Proxy 論文の考え方を、紫苑の既存チャット基盤へ L1 推論時ガイダンスとして実装した。`api/chat_reflection_prompts.py` に `build_world_proxy_prompt_block()` を追加し、記憶・RAG・DB統計・判断学習・経験ループ・グレー判断を「次の一手を決める代理フィードバック」として扱う内部指示にした。通常チャット、RAGチャット、紫苑対話室の共有メモリに接続し、`debug_memory.world_proxy` と prompt/memory usage ログへも記録する。自動学習・自動昇格には接続せず、候補化・検証・人間確認を前提にした。
- User が World Proxy 実装後の回答を「かなり良くなった」と評価した。影響: 既存の記憶/RAG/検証ループを回答前の代理環境として明示する方向は有効。次の行動: 追加拡張より、回答ログと `debug_memory.world_proxy` を見て、過剰注入・自己説明過多・一般論化が出ないかだけ観測する。
- Xudong Han の Self-Evolving Agent 投稿を参考に、重み学習ではなく読み取り専用の経験フライホイール品質ゲートを実装した。`scripts/build_experience_flywheel_report.py` は `prompt_feedback_log`、`shion_experience_events`、`screening_loop_feedback`、`judgment_asset_usage_feedback` を `context/decision/feedback` 候補へ正規化し、`promote_to_review` / `replay_eval` / `observe_only` / `quarantine` に分類する。日次postへ `build_experience_flywheel_report` として接続し、`reports/experience_flywheel_latest.{json,md}` を生成する。自動昇格・skill編集・モデル重み更新はしない。
- 経験フライホイールの `replay_eval` 候補から、既存 `evaluate_answer_quality.py` 互換の評価セット `api/knowledge/experience_replay_eval_set.json` を生成する `scripts/build_experience_replay_eval_set.py` を追加した。釣り雑談やメタ会話は除外し、飲食業リース、事業計画、メイン先、鉄道車両、漁業、焼却炉、ダクト付き空調、工事費、自動車周辺リスクなど10件へ圧縮する。日次postにも接続し、`reports/experience_replay_eval_set_latest.md` を生成する。既存の `answer_eval_set.json` は上書きしない。
- Omar氏紹介の "Catastrophic Remembering" 論文を受け、指示肥大化の読み取り専用監査を追加した。`scripts/build_instruction_debt_report.py` は `AGENTS.md` / `CLAUDE.md` / `.agents/skills/*/SKILL.md` / `MEMORY.md` から理由・適用範囲・削除条件の欠けた指示候補を抽出し、`reports/instruction_debt_latest.{json,md}` を生成する。日次postにも接続したが、自動削除・自動昇格・skill編集はしない。今後の durable instruction には reason / scope / retirement を付ける。
- User が指示負債監査の導入を「かなり良くなった」と評価した。影響: AGENTS/skills/MEMORY の肥大化対策は、自動削除ではなく観測レポート + reason/scope/retirement 付与ルールとして入れる方針が有効。次の行動: 初回レポートは広めに拾うため、上位候補から手動棚卸しし、検出条件を過剰に厳格化しすぎない。
- 指示負債監査の初回棚卸しを実施した。`MEMORY.md` の単なる実装履歴、Markdown説明文、skill frontmatter を過検出しないよう調整し、レポートを `Guidance Debt Items` と `Memory Policy Candidates` に分けた。`AGENTS.md` / `CLAUDE.md` の主要セクションへ reason/scope/retirement を追記し、Obsidian wiki / kb-report の重複した hub 優先ルールにも理由・範囲・見直し条件を付けた。結果: 実データの debt items は 315 → 101、high は 195 → 5 まで減った。自動削除は未実施。
- 指示負債監査の追加棚卸しで、残っていた high guidance を0にした。Cloud Run更新、改善リスト、Obsidian保存、Next/Cloudflare再起動、PDF/OCR変換、改善レポート同期、git-ship、チャット品質比較、自動改善pipeline、AGENTSの会話/heartbeat規約へ section-level の reason/scope/retirement を追加した。理由付きの重複は負債扱いしないよう監査器も調整。結果: debt items は 101 → 32、残りはすべて `MEMORY.md` の `Memory Policy Candidates` で、`Guidance Debt Items` は0。
- User が追加棚卸し後の状態を「かなり良くなった」と評価した。影響: `Guidance Debt Items` を0にし、`MEMORY.md` は削除せず `Memory Policy Candidates` として残す分離が有効。次の行動: 今後の指示負債対応では、まず guidance 側の理由/範囲/削除条件を整え、MEMORY は自動削除せず候補として人間レビューに回す。
- User が今回の改善後の紫苑/記憶運用を「もはやただのRAGではない」と評価した。影響: 価値は検索回答ではなく、指示・記憶・検証・削除条件・人間レビューを含む判断資産運用系にある。次の行動: 今後の説明では、RAG単体ではなく「記憶を使う前に、残す理由と消せる条件まで管理する判断OS」として位置付ける。
- 経験リプレイ評価セットの実測を試した。Gemini API による現在回答生成は外部送信リスクで承認されず、ネットワーク制限時も `generativelanguage.googleapis.com` 名前解決に失敗したため、回避せず停止した。代替として `scripts/evaluate_experience_replay_historical.py` を追加し、ローカルの `prompt_feedback_log.jsonl` に残る当時の実回答を採点した。結果は `reports/experience_replay_historical_quality_latest.{json,md}` に保存し、10件中0件合格、平均61.3、概念カバー53.3%、禁止表現0、不確実性明示漏れ6件。課題は危険発言ではなく、厨房設備/撤退保全、前提根拠、返済原資、鉄道車両の資金調達、漁業の保険担保、焼却炉の設置/撤去/残価などの実務観点不足。
- 経験リプレイ履歴評価の失敗を、回答チェックリスト候補へ変換する `scripts/build_experience_replay_checklist_candidates.py` を追加した。`reports/experience_replay_checklist_candidates_latest.{json,md}` に、グローバル候補（所有権/原状回復/撤去、所有者/使用者、不確実性明示など）とケース別候補（飲食業、焼却炉、自動車、ダクト付き空調、鉄道車両、漁業、事業計画、メイン先）を出す。これはレビュー前提の候補で、プロンプト・記憶・skill へは自動反映しない。日次postにも接続済み。
- 経験リプレイチェックリスト候補にレビュー台帳を追加した。`scripts/review_experience_replay_checklist.py` は `reports/experience_replay_checklist_candidates_latest.json` の候補へ `data/experience_replay_checklist_reviews.jsonl` の最新採否を重ね、`accepted` / `revised` だけを `data/experience_replay_accepted_checklist.json` と `reports/experience_replay_checklist_review_latest.md` に発行する。Reflection Gate はこの active ファイルのみを読み、未レビュー・却下候補は使わない。現時点は候補16件、採用済み0件。
- Memory Engineer 視点を既存 `scripts/build_memory_engineering_report.py` に統合した。Stanford/Microsoft/Anthropic/Nvidia の4レンズに加え、`state_inventory`（active / candidate_or_review / quarantine / rejected / maintenance）、`utility_kpis`（candidate_to_active_pressure、checklist_review_rate、field_feedback_coverage、quarantine_rate）、`forgetting_policy`（raw_logs / candidate_memory / active_but_sleeping / quarantine / contradiction）を出す。影響: 記憶を「増やす」だけでなく、候補圧・隔離・眠っているactive判断資産・忘却レビュー候補を1枚で見られる。次の行動: 自動削除はせず、まず `reports/memory_engineering_latest.md` の上位サンプルを人間レビューに使う。
- `reports/memory_engineering_latest.md` に `Daily Review Focus` を追加した。毎朝見る対象を 1) 候補を少し採否する、2) quarantine が多い抽出元を弱める、3) sleeping active rule を次案件で試すか保留する、の3つへ絞る。影響: Memory Engineer 監査が単なる状態表示ではなく、短時間レビューの入口になった。次の行動: ここに出た項目も自動昇格・自動削除せず、人間レビューと実案件フィードバックで動かす。
- 添付画像「自己メンテ型セカンドブレインの5段階構造」を tune_lease_55 向けに翻訳し、`docs/self_maintaining_second_brain.md` として保存した。影響: 紫苑を単なるRAGではなく、監査、Skill化、Raw/Wiki/Schema構造化、可視化、自動運転で腐らせない判断資産運用OSとして説明できる。次の行動: 新パイプライン追加ではなく、既存の `memory_engineering` / `experience_flywheel` / `instruction_debt` の読み方として使う。
- User が自己メンテ型セカンドブレイン文書化後の状態を「かなり良くなった」と評価した。影響: 紫苑を「記憶を増やすRAG」ではなく、「腐る前提で監査・Skill化・構造化・可視化・自動運転する判断資産OS」と説明する方向は有効。次の行動: 今後の資料化やデモ説明では、この5段階構造を既存パイプラインの意味づけとして使い、新規自動化の口実にはしない。
- 自己メンテ型セカンドブレインをデモ説明へ落とし込み、`docs/shion_second_brain_demo_talk_track.md` を追加した。影響: 30秒説明、3分トーク、スライド構成、画面順、Q&A、使う/避ける表現が揃い、既存ハッカソン資料の裏側にある知識運用軸として話せる。次の行動: デモでは「全自動学習」ではなく「低リスクは自動、高影響判断は人間レビュー」と明確に言う。
- User が「ハッカソンは終わった」と共有した。影響: これまでの freeze/stabilize やデモ保護を最優先する姿勢から、ハッカソン後の棚卸し、実運用向けの整理、本流化/廃止判断へ徐々に切り替えられる。次の行動: デモ用に入れたもの、実務に残すもの、削るものを分け、紫苑の知識運用OSとしての説明と実装を本流目線で見直す。
