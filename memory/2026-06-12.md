# 2026-06-12

## Snapshot
- Next 側の prompt feedback loop を軍師 SSE まで含めて接続した。
- `api/gunshi_gemini.py` に `PDCAあり/なし` の system prompt 差分と `record_prompt_feedback()` を追加した。
- `frontend/src/app/improvement-log/page.tsx` の改善ログ画面は prompt feedback 集計を表示できるようになり、`frontend/` の `npm run typecheck` も通過した。
- 改善ログの各行に「修正登録」ボタンを追加し、押した内容を `pdca_ai_rules.json` の `ai_prompt_addons` に1クリックで追記できるようにした。

## Decisions
- 軍師の SSE 経路も通常チャットと同じく、PDCA 注入と差分ログを残す方針にした。
- 修正の登録はモーダルなしの1クリック追記にし、判断負荷を増やさない。
- 月次レポートは既存の自動集計と Obsidian 連携を維持し、パイプラインの拡張は最小限に留める。

## Promotable
- Prompt feedback は `PDCAあり/なし` と `前回応答との差分` を同じ基盤で記録すると、Next 側でも効果検証しやすい。
- UI 側の集計カードは、改善ループが動いているかを毎回手で掘らずに確認できる入口になる。
- 修正を教える最短経路は、改善ログの「修正登録」ボタンから `ai_prompt_addons` を増やすこと。
