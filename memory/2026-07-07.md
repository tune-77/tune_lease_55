# 2026-07-07

## Snapshot
- User noticed that 紫苑 in `リース知性体との対話室` and 紫苑 in normal `AIチャット` give different answers.
- Investigation found they are separate backend routes:
  - `/api/lease-intelligence/dialogue`: dedicated Lease Intelligence persona, tools, mind/reflection/dialogue memory, Obsidian Dialogue notes.
  - `/api/chat`: normal chat route with `response_mode=shion`, identity memory, experience loop, RAG/debug layers, and chat history.
- Normal chat returned in about 9 seconds for a short identity question with `context_mode=casual`, `identity_memory.used=true`, `experience_loop.used=true`, but `rag_context_used=false` and `obsidian_daily_used=false`.
- Dialogue route timed out from local curl at 60 seconds for the same short identity question; its server-side Gemini tool timeout is up to 90 seconds and the Next proxy timeout is 180 seconds, so it is heavier and can feel different/slower.

## Promotable Items
- AI Chat and Lease Intelligence Dialogue are intentionally different Shion surfaces today. If User expects the same 紫苑, route/persona unification or clearer UI labeling is needed.
- Dialogue Shion was not injecting `user_personal_memory` into its prompt, so it could save/capture personal memory separately but could not reliably answer personal-memory questions like the dog name. Fixed by appending the same personal memory block used by `/api/chat` to `/api/lease-intelligence/dialogue`.
- The personal-memory extractor had incorrectly stored incomplete dog-name text as `Dog name: は`. Removed the bad confirmed fact and tightened extraction so questions/incomplete prompts are not saved as names.
- Dialogue Shion returned `503 Obsidian Vaultが見つかりません` before generation when Vault lookup failed, so subsidy/tax/lease knowledge questions could get no answer despite being answerable from baseline knowledge. Added a no-vault fallback path for `/api/lease-intelligence/dialogue` and expanded `lease_finance_knowledge.py` with subsidy/aid screening basics.
- Cloud Run GCS Vault/RAG sync is healthy on live service (`USE_GCS_VAULT=true`, `/api/system/cloud-status` ready, `/tmp/gcs_vault` markdown_count 119, Chroma document_count 581). Separate issue: Cloud Run DB is demo/readonly, so chat message persistence logs can show readonly DB skips.
- Cloud Run input sync was downloading events and writing summary notes, but `improvement_note` events were not materialized into the normal `Projects/tune_lease_55/AI Chat/Improvement Log` format. This meant improvement pipeline extraction missed user-entered Cloud Run improvement logs.
- Fixed Cloud Run input improvement reflection by adding `improvement_note` handling to `scripts/sync_cloudrun_inputs_from_gcs.py` (`data/cloudrun_improvement_log.jsonl`) and `scripts/sync_cloudrun_inputs_to_obsidian.py` (writes normal Improvement Log sections with `## 原文` / structured candidates and event-id dedupe markers). Backfilled 27 local Cloud Run improvement entries and synced 27 Obsidian improvement sections across 2026-07-01 and 2026-07-03..06. `extract_obsidian_improvements.py` now extracts 51 deduped improvements and includes Cloud Run items such as duplicate AI response, stale system feature list, chat state loss after knowledge-universe navigation, voice stop issue, dialogue Shion memory issue, and result-registration refresh issue.
- Cloud Run normal conversation logs were also not reflected. Root cause: live Cloud Run is configured as `CLOUDRUN_DATA_MODE=demo`, `DEMO_READONLY=1`, `DB_PATH=/app/data/demo.db`, with Cloud SQL instance attached but no `DATABASE_URL` / `CLOUD_SQL_PASSWORD`; `sync_cloudsql_to_obsidian` failed nightly with exit 1 and Cloud Run DB `save_message` logs were readonly skips. Existing normal chat history was therefore likely not persisted in Cloud SQL/GCS.
- Added a GCS fallback conversation path: `/api/chat` and `/api/lease-intelligence/dialogue` now record redacted 1-turn `chat_exchange` events to Cloud Run input writeback; Mac sync materializes them to `data/cloudrun_chat_log.jsonl` and `Projects/tune_lease_55/AI Chat/Cloud Run Conversation Log/YYYY-MM-DD.md`. `sync_cloudsql_to_obsidian.py` now exits cleanly with a warning when Cloud SQL credentials are absent instead of breaking the daily pipeline.
- Audited all existing `data/cloudrun_inputs/*.jsonl` event types: `shion_memory_usage` 155, `improvement_note` 27, `rag_feedback` 19, `score_full_calculated` 9, `screening_loop_feedback` 9. `improvement_note`, `rag_feedback`, `screening_loop_feedback`, and scoring inputs had dedicated materialization after fixes; `shion_memory_usage` was only summarized. Added `shion_memory_usage` materialization to `data/shion_memory_usage_log.jsonl` and expanded wizard-input extraction to include `score_full_calculated`. Backfilled `shion_memory_usage_new=155` and `wizard_new=9`.
- Private Reflection repeated similar content because recent dates had no `Lease Intelligence/Dialogue` file and the generator fell back to canned local reflection text while quoting recent reflections. Since User is not using the Next version/dialogue screen now, Cloud Run conversation data must be treated as reflection input. Updated `lease_intelligence_reflection.py` so `_load_dialogue()` also reads `AI Chat/Cloud Run Conversation Log/YYYY-MM-DD.md`, `Cloud SQL Summaries/YYYY-MM-DD_cloudsql_summary.md`, and `AI Chat/YYYY-MM-DD.md`, and fallback reflections include Cloud Run input event counts while avoiding previous-reflection quote blocks when there is no dialogue material.
- Cloud Run寄せのパイプライン検査で、`run_daily_improvement_core.sh` は Cloud Run input sync → Obsidian反映 → Cloud SQL会話同期 → memory pack → 改善抽出の順。実績ログでは `sync_cloudsql_to_obsidian` が毎晩 exit 1 で落ちていたが、Cloud SQL credential 未設定時は GCS input `chat_exchange` 経路へ逃がして警告終了するよう修正済み。`sync_cloudrun_inputs_to_obsidian.py --dry-run` は既存ローカル入力から 2026-07-05=26件、07-06=95件、07-07=21件をObsidian/Dailyへ反映可能と確認。対象テスト `pytest tests/test_sync_cloudrun_inputs_from_gcs.py tests/test_cloudrun_writeback.py tests/test_shion_memory_freshness.py -q` は 20 passed。
- 結果登録画面の未登録一覧が Cloud Run版の審査入力を拾えていなかった。原因は `/api/cases/pending` が `past_cases.final_status='未登録'` だけを見ており、Cloud Run demo/readonly で GCS input に戻る `score_full_calculated` / 隔離DB `cloudrun_score_inputs` を統合していなかったこと。`api/main.py` に Cloud Run帰還スコア入力の pending 統合、Cloud Run本体用GCS直読み、`cloudrun_score:<id>` の登録時past_cases昇格、`cloudrun_event:<event_id>` のGCS結果登録、削除時の破棄イベント記録を追加。ローカル確認で `/api/cases/pending` 相当は 55件中 Cloud Run帰還候補5件を含む。`python -m py_compile api/main.py`、Cloud Run同期系pytest 20 passed、frontend `npm run typecheck` passed。
- AIチャット履歴が画面遷移で消える問題に対応。Cloud Run demo/readonly ではDB保存が失敗するため、`frontend/src/lib/chatLocalHistory.ts` を追加し、通常 `/chat` と右下 `FloatingMebuki` の両方で server history + localStorage history をマージして表示するようにした。削除ボタンはDB削除に依存せず `cleared_at` をlocalStorageへ保存し、画面から即時消して以後古いserver historyを再表示しない。右下チャット上部にも削除ボタンを追加。frontend `npm run typecheck` passed。
- 紫苑対話室で古い履歴が毎回表示される問題に対応。`frontend/src/app/lease-intelligence/page.tsx` の初期表示で `/api/lease-intelligence/dialogue/state` のDB履歴をそのまま出さず、今日以降かつブラウザ側の `cleared_at` 以降の会話だけにフィルタし、ローカル表示履歴とマージするようにした。削除ボタンは画面から即時消し、Cloud Run readonly等でサーバー削除が失敗しても古いDB履歴を再表示しない。frontend `npm run typecheck` passed。
- Cloud Run版レビュー後の改善を実施。GCS input writeback の戻り値判定を `ok is True` のみに修正し、GCS失敗時に結果登録/破棄を成功表示しないようにした。`api/chat_memory.get_recent_messages` に `since` パラメータを追加し、`/api/chat/history` と `/api/lease-intelligence/dialogue/state` も `since` を受けるようにした。フロントの通常チャット、右下チャット、紫苑対話室は当日/削除後時刻を `since` として渡すため、Cloud Run固定user_idの古い共有履歴がサーバから返りにくくなった。結果登録画面のGCS直読みは短TTLキャッシュ化し、登録/破棄成功時に無効化する。`python -m py_compile api/main.py api/chat_memory.py`、frontend `npm run typecheck`、Cloud Run同期系pytest 20 passed。
