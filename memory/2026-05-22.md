# 2026-05-22

## Snapshot
- Context: legacy raw daily note.
- Action: captured the day's operational details below.
- Result: keep the full log intact for later review.
- Next: promote only repeated or decision-shaping items into `MEMORY.md`.

- Built a daily lease-news collector that pulls Google News RSS, deduplicates items, writes an Obsidian digest note, and appends a short summary to `Daily/YYYY-MM-DD.md`.
- Added a `launchd` agent for 07:20 daily execution and installed it into `~/Library/LaunchAgents`.
- Added an Obsidian project note for the collector plus a daily note entry so the workflow is searchable later.
- Expanded the lease-news collector to use industry-specific query sets and official RSS feeds from METI, FSA, and MLIT. The launchd job now runs the `industry-watch` profile by default.

## Promotable Items
- Preserve exact decisions, failure modes, and verification details when they matter.
- If a rule repeats across days, it should move to `MEMORY.md`.
