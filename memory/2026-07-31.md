# 2026-07-31

- 自己言及質問への個性付与を実装した。`/api/chat` の紫苑モードで「存在価値」「役割」「何のため」などを自己言及トリガーに追加し、一般AIの機能論ではなく「Userのリース判断を一回限りの勘で終わらせず、判断資産へ残すためにいる」という一人称のスタンスを返すよう補強した。対話室側にも同じ境界を追加し、意識の自己申告やPrivate Reflection漏えいは避けつつ、公開できる設計思想として紫苑らしさを出す方針にした。
- 記憶を参照しているのに抽象的・紋切り型に聞こえる問題への対策を実装した。`api/main.py` に `memory_expression` プロンプトを追加し、記憶・RAG・過去案件・判断資産を使う時は「どの種類の過去経験が、今回の判断のどこに効いたか」を最大1文で具体化するようにした。乱用防止として、毎回「前回は」「以前は」で始めず、社名・個人名・生財務数値・Private Reflection原文は出さない。`lease_intelligence_dialogue.py` にも同じ表現ルールを入れ、`tests/test_shion_specificity_prompt.py` で発火/抑止を検証した。
- Userから「紫苑の自己提案はなかなかいい」と評価があった。影響: 紫苑の自己提案は単なる改善候補ノイズではなく、Userに価値が出始めている観測対象として扱う。次の行動: 自己提案は件数を増やしすぎず、根拠層・採用/保留/却下・実際に効いたかを追って質を維持する。
- Userから「自己提案その方向で育ててくれ」と依頼があり、自己提案の品質管理を実装した。`scripts/attach_shion_self_proposals_to_report.py` で自己提案に `quality_score`、`proposal_maturity`、`review_status_label`、`effect_tracking` を付与し、レポートでUserに出す候補は上位3件へ制限した。対話室文脈も上位3件・根拠層・効果確認を読むようにし、提案APIの既定表示件数も3件に抑えた。
- Userが自己提案「FAQ/ヘルプ情報の集約と再配置」を提示した。判断: `/faq` と `/help` の低利用だけで削除するのは根拠が弱く、まず「文脈導線不足」と見て採用する。実装: サイドバーにサポートハブを出し、審査画面・紫苑対話室から `/help`/`/faq` へ導線を追加し、FAQ側にもサポートハブへの戻り口を追加した。
- Userが「自己提案はこの判定まで持っていけると、かなり実務で使える」と明確に同意した。影響: 自己提案の価値基準は、単に改善案を出すことではなく、低利用や不満の原因を「価値不足 / 発見性不足 / 文脈不足 / 削除候補 / 観測継続」に切り分け、削る・移す・残す・測るを判定できることに置く。
- Userが、紫苑の自己提案は「こちらが意図している内政モードに近いものがあっていい感じ」と評価した。影響: 自己提案は外向きの審査回答ではなく、紫苑がシステム内部の導線・情報配置・記憶運用・改善候補を見回し、採用/保留/削除/観測継続を提案する内政モードとして育てる。次の行動: 自己提案UIや対話文脈では「内政」らしく、根拠、影響範囲、実行可否、観測指標を短く揃える。
- Userが、内政モードのために紫苑が使う必要情報を与えたいと話した。実装: `docs/shion_domestic_mode_inputs.md` を追加し、重要度、使用文脈、意図、判断基準、触らない線、成功指標、再判定時期を入力項目として定義した。自己提案レポートの `quality_policy` と対話室文脈にも、これらをログより強い補正情報として扱う指示を入れた。
- Userが「CodexとClaudeの作業録も要約して渡すと、Userが考えた内容が紫苑にもわかるのでは」と提案した。実装: `scripts/build_agent_worklog_digest.py` を追加し、通常VaultのDailyにある Codex/Claude Work Log から Summary / Chat Summary / Decisions / Changes / Verification / Open Items だけを抽出して `reports/agent_worklog_digest_latest.{json,md}` に保存する経路を作った。対話室の改善文脈にも接続し、日次改善パイプラインで警告扱い生成するようにした。
- Userが「内政モードはどこにある？」と確認し、その後「作って」と依頼した。実装: `/improvement-log` の自己提案カード群の前に「紫苑 内政モード」パネルを追加し、入力源、判定ラベル、出力方針、内政メモテンプレートコピー、紫苑対話室への導線を置いた。影響: 自己提案を単なる改善カードではなく、システム内部を見直す明示的な運用モードとして扱えるようになった。
- Userが、内政モードに「内政メモを貼ったあと紫苑が採用/保留/却下/観測継続まで返す小さな入力欄」を追加する方向に同意した。実装: `/improvement-log` の内政モードパネルに内政メモ入力欄、テンプレート挿入、紫苑の初期判定カードを追加した。初期判定は意図、使う場面、残す理由、削ってよい条件、触らない線、成功指標、再判定時期を見て、採用候補/保留/却下候補/観測継続を返す。
- Userが「実際に紫苑が考える行動は何で考えてるんだ。Gemini/Codex/Claude？」と確認し、初期判定もGeminiに渡す方向に進めることになった。実装: `api/domestic_mode.py` と `/api/domestic-mode/evaluate` を追加し、内政メモと画面内一次判定をGeminiへ渡して、採用候補/保留/却下候補/観測継続、判断層、不足入力、成功指標、再判定時期を返すようにした。`/improvement-log` には「紫苑で再判定」ボタンと結果表示を追加した。
- Userが「紫苑再判定の結果を自己提案ログに保存して、あとで採用・保留・却下の効果追跡までつなげる」方向に同意した。実装: Gemini再判定成功時に `data/domestic_mode_proposals.jsonl` と `data/cloudrun_improvement_log.jsonl` へ `surface=shion_self_proposal` / `source=domestic_mode` として保存する経路を追加した。自己提案レポート取り込みにも `domestic_mode_based` レイヤーを追加し、内政モード由来の再判定を既存の上位3件・品質点・効果追跡へ乗せた。
- Userが、保存された内政モード提案に対して `/improvement-log` 上で直接「採用 / 保留 / 却下」を押せるようにすることを依頼した。実装: Gemini再判定結果の保存カードに採用・保留・却下ボタンを追加し、既存の `/api/improvement-log/review` へ canonical_key/title/reason を送って台帳・Obsidian・writeback に判断を記録するようにした。
- Userが「メモ入力が面倒くさそう」と指摘した。実装: `/improvement-log` の内政モード入力を、対象・意図・成功指標の3項目を基本にし、導線改善/削除検討/観測継続/記憶運用のプリセットで意図と指標を自動補完できるようにした。使う場面、残す理由、削ってよい条件、触らない線、再判定時期は「詳細を足す」に折りたたみ、生成された内政メモは確認用の折りたたみにした。
- Userが「内政モードで判断メモリが増えると紫苑の自己提案モードも改善されるのか」と確認し、自己提案生成への注入を依頼した。実装: `api.domestic_mode.build_domestic_memory_context()` を追加し、内政モード提案と改善ログ上の採用/保留/却下状態を短く要約して、Usage / Feedback Pattern / Judgment Divergence / Outcome Drift / Knowledge Gap の各自己提案Geminiプロンプトへ渡すようにした。効果: approved の判断軸を優先し、rejected の浅い提案を繰り返さず、guardrail/触らない線を守る方向へ寄せる。
- Userが「自己提案モードで出た提案を内政モードに自動入力して使う」「すべて内政モードにつながるように、ボタンはつけなくていい」と依頼した。実装: `connect_self_proposal_to_domestic_mode()` を追加し、Usage / Feedback Pattern / Judgment Divergence / Outcome Drift / Knowledge Gap の各自己提案生成時に `data/domestic_mode_proposals.jsonl` へ自動接続するようにした。自己提案結果には `domestic_connection` を返し、重複は canonical_key で抑制する。ボタン操作なしで、自己提案→内政判断メモリ→次の自己提案のループに入る。
- Userが「たまに突拍子のないことも言って欲しい」「そのほうが面白い」と希望した。実装: 自己提案レポートに `leap_limit=1` を追加し、上位3件のうち最大1件だけ `proposal_style=leap` または跳躍/実験/違和感などの探索提案を混ぜるようにした。各自己提案Geminiプロンプトにも、最大1件だけ検証可能な跳躍提案を許可し、success_metric と risk を必須にした。実務改善2件＋跳躍枠1件のバランスを狙う。
